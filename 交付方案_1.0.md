# Keel 1.0 交付方案

> 回答三件事：**① 交付什么产品 ② 还差什么 ③ 怎么补**。
> 缺口来自本次实测（不是推测）：见 §2 每项的「实测依据」。
> 日期：2026-09-15

---

## 1. 最终交付什么产品

### 一句话

> **Keel 1.0 = 剧本 / 微短剧的「结构体检 + 合规自检」工具。**
> **形态：一条命令 `keel "一句话想法"` → 30 秒内产出一份能双击打开、能发给别人的 HTML 报告。**

### 不做的事（四条红线，都是硬约束）

| 不做 | 为什么 |
|---|---|
| **不做网文代笔** | F6：起点/番茄/晋江 一致禁止 AI 直出正文。**同一 Keel：做网文是违规工具，做剧本是合规工具。** 已实现为 `keel/policy.py` 闸门 |
| **不预测爆款** | LLM 评委与人类判断**反相关**（会把零样本 AI 故事排在《纽约客》之上）。只做**结构性淘汰** |
| **不做反检测** | 机检红线（`tests/test_redlines.py`）。技术上徒劳、伦理上站不住、商业上致命 |
| **不自动改结构** | 只自动修**文风**；结构类出提案等人裁决。理由一：保护作者意图。理由二（PLOTTER 实测）：图规划路线在前提忠实度上只有 **40%**，经验上也做不好 |

### 卖点一句话

> **避免浪费。**

不是「帮你写得更快」—— 那是在加剧那 **98.7%**。
是「在投入制作之前就知道这部结构上有问题」。
依据：2026 H1 22.19 万部 AI 短剧，**破亿率 0.48%**，**98.7% 半年内未回本**。

**因此定价不应按生成量**（那等于激励生产更多失败品）。

### 三条差异化（每条都有外部证据）

| 差异化 | 外部证据 | Keel 的实现 |
|---|---|---|
| **承诺可校验** | NCP-Bench（ICML 2026）：最强模型 GPT-5.2 在 20 轮后**存活率仅 42%**，事实冲突率 40–68% | L3 承诺层是 IR 一等公民，因此**可验算**而不是靠提示词祈祷 |
| **前提不漂移** | PLOTTER（ACL 2026）：图规划四维度胜率 72–100%，**唯独前提忠实度 40%/14%/44%** | `premise_fidelity` 校验器 |
| **觉察可见** | CHI 2026 *Reactive Writing*（19 人访谈 + 1,291 次会话）：作者**察觉不到** AI 的影响，却感觉完全掌控 | `awareness.py` — 在**写作时**回显「已做判断 N 次（采纳 X / 驳回 Y）」 |

### 目标用户

1. **微短剧 / 剧本创作者**（主）—— 合规路径，且下游失败率创造了质量需求
2. **网文作者**（次）—— 只用结构 + 伏笔表 + 人设卡 + 合规自检，**不出正文**
3. **编剧 / 叙事设计**（2.0）—— Ink / Ren'Py 导出

---

## 2. 距离交付还差什么（实测）

### 实测发现的硬缺口

| # | 缺口 | 实测依据 | 级别 |
|---|---|---|---|
| **G1** | **不可安装** | 无 `pyproject.toml` / `setup.py`，无 console script。用户必须自己建 venv、装 pydantic、clone 仓库、敲 `python -m keel.cli` | 🔴 阻塞 |
| **G2** | **默认路径不产出可见物** | `write` 不传 `--out` 时**一个文件都不写**（`cli.py:83`）—— `report.html` 根本不会出现。**第一分钟路径是断的** | 🔴 阻塞 |
| **G3** | **不自动打开报告** | 只打印路径，用户要自己找文件双击 | 🟠 高 |
| **G4** | 无 `python -m keel` | 缺 `keel/__main__.py` | 🟡 中 |
| **G5** | 版本号仍是 `0.2.0` | `keel/__init__.py:52` | 🟡 中 |
| **G6** | **P2.2 `max_rounds` 未测** | 离线测不出（`MockGenerator` 的 revise 返回 `changed: False`） | 🟡 中 |
| **G7** | 无用户文档 | 只有面向开发者的 README，无 QUICKSTART | 🟠 高 |
| **G8** | 无 CHANGELOG | 交付物应有版本记录 | 🟢 低 |

### 不是缺口（已确认可用）

- ✅ 代码无 `TODO / FIXME / NotImplemented`（全库 grep 为空）
- ✅ `litellm` 是**懒加载**，离线环境导入不受影响
- ✅ 无参数时打印 usage，不崩
- ✅ 29 门禁 + 7 报表项，自证 62/62
- ✅ 6 个渲染器（含 HTML）、54 模块导入 0 失败、两个端到端 demo 全过

### 交付前**不建议**做的（避免过度工程）

| 不做 | 理由 |
|---|---|
| Web UI / SaaS | 部署 + 鉴权 + 成本，是与当前规模不匹配的大跃迁。先用「可安装 + 自动打开报告」把第一分钟跑通，再据真实反馈决定 |
| 账号 / 计费 | 同上 |
| 更多校验器 | 29 个已足够；瓶颈是**能不能被看见**，不是检查数量（铁律：35 个校验器没人看 = 0 个） |

---

## 3. 补齐计划（本次全量推进）

**A1–A8 全部完成（2026-09-14）。**

| 序 | 工作 | 补哪个缺口 | 状态 / 实测结果 |
|---|---|---|---|
| **A1** | `pyproject.toml`（含 console script `keel`） | G1 | ✅ 装出 `keel.exe`；另在**全新 venv** 里装 wheel 并从零目录跑通 |
| **A2** | `keel/__main__.py` | G4 | ✅ `python -m keel --version` → `keel 1.0.0` |
| **A3** | `write` 默认输出目录 | G2 | ✅ 不传 `--out` 写入 `out/<想法前 40 字>/` |
| **A4** | 自动打开报告（`--no-open` 可关） | G3 | ✅ 打印 `🌐 已用默认浏览器打开` |
| **A5** | 版本号 → `1.0.0` | G5 | ✅ 且版本漂移已机检（见 A9） |
| **A6** | `QUICKSTART.md` | G7 | ✅ 文中每条命令均实测；两处写错的地方已按实测改正 |
| **A7** | 测量 `max_rounds` | G6 | ✅ 拿到数字，**但明确不改默认值**（见下） |
| **A8** | `CHANGELOG.md` | G8 | ✅ 注明「无 git，系重建」 |
| **A9** | 打包一致性机检（本轮新增） | — | ✅ 见下 |

### A9：打包过程中发现的三个真缺陷

做 A1 的验收时暴露的，都不是理论问题：

1. **`packages` 是手抄列表** —— 新增子包忘了同步会**静默**从发行包里消失
   （本地全绿、装出去缺模块）。改成 `packages.find`。
2. **`Homepage` 是个不存在的 GitHub 地址** —— 本仓库连 git 都没有。
   直接删掉（写一个假 URL 是声明不存在的事）。
3. **声明了 MIT 却没有 LICENSE 文件** —— 只有元数据、没有授权正文。补上 `LICENSE`。

然后给这三点加了机检（`scripts/verify.py` 第 9 节），4 个变异全部变红：
改版本号、删 LICENSE、退化成手抄列表、把 `include` 收窄。

### A7 的做法（关键设计）

`max_rounds` 之前测不出，是因为 `MockGenerator` 的 revise **设计上就不改写**。
解法不是换真 LLM，而是**注入一个会真改写的测试用 reviser** ——
测的是**循环机制**（收敛性 / 迭代退化），不测 LLM 质量。

这样得到的结论必须标注清楚：

> 本测量只覆盖**循环机制**（多少轮收敛、迭代是否退化），
> **不覆盖**生成质量 —— 后者必须接真 LLM 才能测。

---

## 4. 交付验收 —— 实测结果（2026-09-14）

```
pip install -e .              成功    ✅ keel-narrative-1.0.0
keel --help                   可用    ✅
keel --version                可用    ✅ keel 1.0.0（本轮补的，之前会报"缺少子命令"）
keel "一句话想法"              → 自动打开 HTML 报告    ✅ 约 2 秒（离线桩件）
report.html                   自包含、断网可看    ✅ 实测 0 外链、无 <script>，8.1 KB
scripts/verify.py             全绿    ✅ 67 / 67（本轮 62 → 67）
两个端到端 demo                全过    ✅ pipeline_demo / demo 均 exit 0
QUICKSTART.md                 新用户可照做    ✅

附加（比原标准更严的一条）：
  全新 venv + wheel 安装 + 零目录运行   ✅ 证明"别人装得上"，不依赖当前工作区
  全模块导入冒烟                        ✅ 55 个模块，0 失败
```

### 验收时写错、然后按实测改正的两处

记下来是因为这正是文档最容易骗人的地方 —— 写的时候觉得对，跑一遍才发现不是：

1. QUICKSTART 原写 `keel write --medium web_novel` 会抛 `PolicyError`。
   **实测不是**：CLI 把这层转成可读的警告，跳过正文，**退出码 0**。
   `PolicyError` 只在直接调 `render_text()` 时抛。
2. QUICKSTART 原写 `keel decide <ir.json>` 逐条裁决。
   **实测不是**：不给 id 也不给 `--all` 时它提示
   「没有指定要裁决的提案（用 --all 或给出 id）」。

另外原稿里"已做判断 12 次（采纳 5 / 驳回 7）"是我编的示例数字，
已改成 `N 次 / M 条` 的占位形式 —— **文档里不该出现看起来像实测的假数字**。

---

## 5. 1.0 之后（不在本次范围）

- **2.0 运行时**：Ink 首发 → Ren'Py（渲染器已实现，是排序问题不是开发问题）
- **真 LLM 端到端验证**：需要 API key
- **`max_rounds` 的真实质量测量**：需要真 LLM
- **交付形态升级**（Web / 桌面）：等真实反馈
