# 工程实现计划 2.0（2026-09-17）

> 配套文档：《产品战略与顶级思考_2026-09-17.md》。
> 本计划只列**有外部证据支撑**的改动。每条都写明：为什么（证据 + 级别）、
> 做什么（模块级）、验收（**可机检**，不是"感觉好了"）、风险。
> **本文件中的数字一律从源码派生或由 verify.py 断言，不手写**（铁律 12/17）。

---

## 0. 现状基线（2026-09-17 实测派生）

| 项 | 值 | 来源 |
|---|---|---|
| 注册项 | 44（**35 门禁校验器 + 9 报表项**） | `registry_stats()` |
| 结构模板 | 13 | `list_templates()` |
| 流水线引擎 | 10（含 `run()` 的顶层类） | `ast` 扫 `engines.py` |
| 版本化提示词 | 8 | `REGISTRY.all()` |
| 渲染器 | 8 | `render.__all__` |
| 反例变异 | 由 `len(MUTATIONS)` 派生 | `tests/fixtures.py` |
| 自证 | **通过（81 项断言，0 失败）** | `scripts/verify.py` |
| 依赖 | **仅 pydantic**；无 API key 可端到端跑 | `requirements.txt` |

已具备而**别的项目没有**的东西：三层正交 IR、永不静默改写的提案协议、
决策留痕（机器提出/人类裁决 + 墙钟）、35 个可溯源到方法论的校验器、
起飞前体检、预算闸、原子检查点、全局漂移检测。

---

## 1. 工程原则（沿用 + 本轮新增）

**沿用（违反过就出事）**：IR 是本体、三层不可合并、校验器须溯源方法论、
`extra="forbid"`、提示词软代码硬、无 key 可跑、不预测成功、中文并列体系、
门禁 vs 报表项分类可机检、SKIPPED≠PASS、净命中率靠反例、数字不手写、
先红后绿 + 变异测试、并行派发看文件所有权、新增门禁须交三件套、
**非结构信号与结构分混用待决**（§决策 5）。

**本轮新增三条**：

- **P-21 · 面向人的入口不许抛裸异常。** traceback 是把调试成本转嫁给不懂调试的人。
  任何 CLI 子命令、Web 路由，出口必须说人话并给出下一条该敲什么。
  （已修：`_load()`、`_interactive_menu()` 的 EOFError。）
- **P-22 · 校验器自己也要被校验。** 外部证据：ConStory-Checker 准确率仅 67.8%（[C]，
  未核原文）。我们的 35 个校验器**没有 precision/recall 报告**。
  没有自检的门禁 = 用不可信的尺子量别人。
- **P-23 · 修复必须有预算，且要与长度控制解耦。** 外部证据：ConWriter（EMNLP 2026）
  在 GPT-5 / 6K–12K 上因"修复遵从 vs 长度控制"冲突**不收敛**。
  验证器与生成器会互锁 —— 不设预算，系统会在"修不完的修复循环"里烧完 token。

---

## 2. 分期计划

### P0 · 可达性（1.1，6–8 周）—— 最高杠杆，先做这个

**为什么**：P2（可达性是乘数）。InkOS 37 维审计、10 agent，横评只排 #8，
原因就是本地 CLI 高门槛。**再深的门禁，第一分钟看不到 = 0。**
本轮实测：裸跑 `loom` 会抛 traceback（已修）；web 默认路径要人**粘 JSON**（未修）。

| 做什么 | 文件 | 说明 |
|---|---|---|
| 一键启动 | `scripts/loom.bat` / `loom.sh` + `zipapp` 或 PyInstaller | 目标：**双击即用**，不需要用户懂 python/venv |
| Web 默认路径修好 | `loom/web.py` | 默认驱动不能是"要人粘 JSON 填 10 轮"的通道；或至少改掉"最省事，普通人也能用"这句**不成立的文案** |
| 首屏只留一件事 | `loom/render/hub.py` | 命令面板收进"高级"；产物只突出报告，其余进"全部文件" |
| 报告即商品 | `loom/render/report.py` | 单文件、自包含、可分享（对标 AIScriptReader $39/份的形态） |

**验收（可机检）**：
- `verify.py` 新增断言：裸跑 `loom`（stdin 关闭）退出码为 2 且**不含 "Traceback"**；
  `loom audit 不存在.json` 输出含"找不到 IR 文件"且**不含 "Traceback"**。
- Web 首页首屏可见选项中，**不含**"粘贴 JSON"字样（除非文案已改为面向 AI 通道）。
- 枢纽页首屏命令条目 ≤ 3（其余折叠）。

**风险**：打包会引入新依赖与体积；若用 PyInstaller，`--onefile` 启动慢但体验最好，需实测。

---

### P0.5 · 修掉已知假门禁（必须单独立项，可与 P0 并行准备）

**问题**：`StructureEngine._bind_commitments`（`engines.py:401`）无条件执行
`c.satisfied = True`，把"把承诺**排期**到某场"当成"承诺已**兑现**"。
后果：`commitment_satisfied`（L3 防漂离论点，**本项目存在的理由**）的判据
`if not c.satisfied` 恒为假 → **空转的假门禁**。

**为什么现在必须修**：它是我们对外宣称的核心能力。一个空转的门禁，
比没有这个门禁更糟——它让体检报告显示"全部已兑现"，是**向用户说假话**。

**做什么**：
1. 实现真正的"兑现判定"：承诺语句与 `must_hold_at` 场景的结构字段（
   turning_point / goal / state_deltas / value）做**可解释的匹配**，并给出匹配依据。
2. 重算全部基线（预期：6 条 ERROR 级承诺立刻报警，基线分大幅下降）。
3. 先在**新路径**上验证语义正确性，再切全局。

**验收**：
- 新增反例 fixture：承诺已排期但**未兑现** → 必须升级为 ERROR（严重度升级判据）。
- 干净基线（人类已兑现版本）→ 不报。
- `verify.py` 全绿 + 基线分数变化在 CHANGELOG 里**显式记录**。

**风险**：基线分数下降会被误读为"产品变差了"。必须在报告里说明这是**从假绿变真绿**。

---

### P1 · 动态叙事记忆（1.2）—— 消融证明比校验更关键

**为什么**：ConWriter（EMNLP 2026，arXiv 2608.05169）消融：
去掉符号校验 CED 0.1335→0.6843；**去掉动态记忆 →0.7499**。
**记忆的贡献大于校验**，而 Loom 目前**没有**这一层。这是最大的能力缺口。

**做什么**：新建 `loom/pipeline/memory.py`
- `NarrativeMemory`：场景级增量状态（谁在哪、知道什么、欠什么、承诺进度）
- 每写完一场就**提交** `state_deltas` 到记忆，后续场只注入**与当前相关的**记忆
  （沿用 Scripter"只注入相关 lore，绝不注入全文"的既有纪律）
- 记忆本身进 IR（可持久化、可重跑）—— 否则又是一份内存态，与 P0 冲突

**验收**：
- 单元测试：20 场序列中，第 3 场埋的事实**在第 18 场仍能被检索到**（长程召回）。
- 反例：把记忆清空 → 一致性类校验器报错数**显著上升**（严重度升级）。
- 与现有 `LoreSeeder` 的边界写清（lore 是**世界知识**，memory 是**叙事状态**，不合并）。

**风险**：与 LoreSeeder 职责重叠，容易长成第二个 lore。边界必须在 docstring 里写死。

---

### P2 · 显式状态转移语义（1.2）

**为什么**：ConWriter 用 Pre/Post/Forbidden 三分做符号校验（V=0 才接受）。
Loom 现有 `StateDelta` 只有 before/after，**没有 Forbidden（禁止态）**，
也**没有前置条件校验**——所以抓不到"死人复活"这类硬冲突的全部形态。

**做什么**：`loom/ir/models.py` + `loom/validators/structure.py`
- `StateDelta` 增加可选的 `forbidden` 语义（或新增 `TransitionRule`）
- 新增校验器：事件的前置条件在上一个事件后不成立 → ERROR
  （**须交三件套**：`REQUIRES` 声明 + 反例 fixture + 文档计数更新）

**验收**：反例——角色在 sc3 已死亡，sc7 又作为 focalizer 出现 → ERROR 且指出 sc3。

---

### P3 · 修复收敛保障（1.2）

**为什么**：ConWriter 在 GPT-5 / 6K–12K 上**不收敛**（修复遵从 vs 长度控制冲突）。
Loom 的 `CriticLoop` 目前有 `rounds` 上限，但没有**冲突消解**与**长度解耦**。

**做什么**：`loom/engines.py::CriticLoop` / `loom/pipeline/auto.py`
- `RepairBudget`：修复次数、token、时长的硬上限；触顶即**停止并报告**（不静默继续）
- **冲突消解规则**：同一字段被两条建议同时修改 → 记为 `conflicted` 提案，交人裁决
  （复用已有的 `DiffStatus.CONFLICTED` 语义，不新造概念）
- 长度控制与修复**解耦**：修复轮不重跑字数约束

**验收**：反例——构造"两条互斥建议" → 产出 conflicted 提案而非无限循环；
`rounds` 触顶时 `AutoResult.stop_reason` 有明确值且日志说明了代价。

---

### P4 · 校验器自检（1.2）—— credibility 不再裸奔

**为什么**：外部证据显示自动一致性检查器准确率可能只有 ~68%（[C]）。
我们没有 precision/recall 报告，却在向用户报"结构分 89"。

**做什么**：`scripts/verify.py` 扩展 + `tests/`
- 为每个校验器维护 **正例集**（应报）与**负例集**（不应报）
- 输出每个校验器的 precision / recall / **误报样本**，纳入体检报告的"门禁可信度"段
- **诚实标注**：自检集是人造的，不是真实分布——结论只能用于比较，不能当绝对准确率

**验收**：`verify.py` 断言每个门禁校验器**至少**有 1 正例 + 1 负例（缺则红）；
报告展示"门禁可信度"字段。

---

### P5 · 商业化工程（1.3）

| 做什么 | 说明 |
|---|---|
| 按作品的报告导出 | 单文件 HTML/PDF，带作品指纹与生成参数；**离线可验**（本地不依赖服务端） |
| 证据包导出 | 创作过程报告 + 决策留痕 + IR 快照，打包为一个 `.zip`（对标申诉材料） |
| 备案材料（A 层） | 面向微短剧制作的标识/备案辅助输出（第 34 条每集标识 → 生成标识清单） |
| 插件通道 | VS Code / Cursor / Obsidian 插件（对标蛙趣拼文），只做**薄客户端**，逻辑全在本地引擎 |

**验收**：报告自包含（断网可开、无外链，沿用既有纪律）；证据包可重放（导入后重跑校验得同分）。

**风险**：备案材料涉及法规解释，必须**只做清单不做保证**，并附"以主管部门口径为准"。

---

### P6 · 2.0 互动叙事（后续）

Ink / Ren'Py 渲染器已存在（6 个渲染器中的两个），运行时已有（Waypoint + Storylet + Director）。
补齐方向：分支一致性校验（同一 IR 的两条分支不能互相矛盾）、存档与 IR 版本绑定。
**这是新媒介，且能完全避开网文正文红线**。

---

## 3. 关键设计与取舍（写明，不藏在代码注释里）

1. **记忆 vs lore 不合并**：lore = 世界知识（常驻/触发），memory = 叙事状态（随场推进）。
   合并会让"注入什么"这件事失去判据。
2. **报告导出必须离线可验**：一旦依赖服务端签名，就变成了"信不信我们"；
   离线可验 = 谁都能复算，这才是可验证的本义。
3. **按作品计费 → 报告必须能被当作商品交付**：所以报告要单文件、可分享、有作品指纹。
4. **插件只做薄客户端**：逻辑进插件 = 多一份实现 = 多一处漂移。
5. **备案材料只做清单不做保证**：Loom 不是法律意见提供者（铁律 7：不宣称做不到的事）。

---

## 4. 不做什么（写下来，防止被"机会"拉走）

- **不做反检测 / 去 AI 味**：技术上行不通（检测在叙事层）、伦理不可辩护、
  商业自杀。2026 年 Yale/Curtin 停用 AI 检测进一步证明这个方向在退潮。
- **不做爆款预测**：LLM-as-judge 与人类判断**反相关**。
- **不做网文正文直出**：平台红线（番茄纯 AI 永久封号），且中国作家网已完全关闭
  AI 辅助投稿通道。
- **不做纯 Web SaaS**：会被卷进字数战，且背弃隐私定位。
- **不把 C2PA 当卖点**：纯文本侧 2026 年基本无支持，它是占位不是能力。

---

## 5. 度量：怎么知道做对了

| 层级 | 指标 | 判据 |
|---|---|---|
| 门禁可信度 | `verify.py` 通过数 + 每个校验器的 precision/recall | 只增不减；新增门禁必须交三件套 |
| 可达性 | 从"零"到"第一份报告"的**步数与耗时** | P0 目标：≤2 步、≤60 秒、无需读文档 |
| 技术能力 | 长程事实召回率（20 场序列） | P1 后应显著上升，且**有反例证明测得到** |
| 商业 | 按作品报告的**导出数**与**分享数** | 先于收入看导出数（收入滞后） |
| 诚实 | 体检报告是否同时报"评估覆盖率"与"门禁可信度" | 缺一即视为隐瞒（铁律 10/22） |

---

## 6. 排期建议（依赖顺序，不可调换）

```
P0 可达性 ──┐
            ├── P1 记忆 ── P2 转移语义 ── P3 修复收敛 ── P4 自检 ── P5 商业化 ── P6 2.0
P0.5 假门禁 ┘（可并行准备，但**注册与基线重算必须串行**）
```

`P0.5` 与 `P1` 都要动共享状态（`REQUIRES` 表、`tests/fixtures.py`、文档计数、
基线分数）—— 按铁律 18，**注册工作串行**，由集成者一处完成，合并后从集成者视角重跑全量。
