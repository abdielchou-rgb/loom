# 外部调研 · 第二轮（2026-09-14）

> 范围：学术前沿（CHI / ICML / ACL 2026）、产品与留存基准、中国短剧市场与合规三层。
> 方法：逐源抓取原文，不依赖二手转述。每条结论标注证据等级。
> 前置：本文是 `外部调研_2026-09.md`（第一轮，方法论遍历）的补充，不重复其内容。

---

## 0. 先给判断（五条）

1. **Loom 的核心赌注被独立证实了。** ICML 2026 把「叙事承诺保存」正式问题化，并测出
   **最强模型（GPT-5.2）在 20 轮后只有 42% 存活率**。Loom 的 L3 承诺层不是自创概念，
   它是当前学界公认的失败模式。**这是本轮最重要的发现。**
2. **但这个证实是双刃的。** 论文证明的是「强模型也守不住承诺」，**不是**「守住承诺的作品
   就更卖钱」。前者支持 Loom 的技术正确性，后者才支持商业价值。第 4 节的市场数据说明
   后者**尚未被证明**——不要在文档里把两者混为一谈。
3. **PLOTTER 的短板正是 Loom 的机会。** 图结构迭代精炼系统在「前提忠实度」上只有
   **40% / 14% / 44%** 的胜率——图越精炼，越偏离原初前提。Loom 的 `CriticLoop`
   「只自动修文风、不改结构」这条边界，现在有了**第二条理由**：不只是保护作者意图，
   还是防止前提漂移。而 L3 正是防漂移的机制。
4. **「反应式写作」说明 Loom 的溯源做对了一半。** CHI 2026 发现作者**没有察觉** AI 的
   影响却**坚信自己在掌控**。Loom 记录 `judgement`，但这批记录目前只用于**事后申诉导出**。
   要构成真正差异化，它必须在**写作过程中可见**——见第 5.1 节，这是本轮最强的产品建议。
5. **产能不是瓶颈，筛选才是**（本条是对第一轮的**独立印证 + 补强**，非新发现）。
   2026 上半年 22.19 万部 AI 短剧，**破亿率 0.48%**，**98.7% 未回本**（第一轮 §2.1 已记录）。
   本轮新增的证据是**剪刀差**：AI 仿真人剧数量占比冲到 70.62% 而播放份额从 80% 跌到 68%，
   同期 3D 漫用 16 倍播放增长吃掉份额——**产量与播放不成正比**。
   如果 Loom 定位成「帮你写得更快」，它就在加剧那个 98.7%。
   定位成「在写之前就知道哪部不值得写」，它才有 M3 之后的留存。
   ⚠️ 注意：决策 #2 目前记的是「观众模拟**不作为卖点**」（依据：LLM 评委与人类判断反相关）。
   本轮数据**不支持推翻它**——但支持一个更精确的表述：
   观众模拟不当卖点，但它是「避免浪费」这套叙事的**技术底座**。
   卖点是「避免浪费」，不是「预测爆款」。见 §5.2 决策点 D。

---

## 1. 证据分级说明

| 等级 | 含义 | 用法 |
|---|---|---|
| **A（硬）** | 已发表论文 / 一手官方文件 / 带明确口径的一手数据 | 可写进 README、可引作设计依据 |
| **B（软）** | 权威机构分析文章、有数据但未公开方法论 | 可作判断依据，不可作对外宣称 |
| **C（勿引）** | 二手转述、营销稿、榜单文 | 仅用于启发，不进入任何文档 |

本文只有 A / B 两级进入结论。第 6 节列出被我剔除的 C 级内容。

---

## 2. 学术前沿

### 2.1 NCP-Bench：承诺保存被正式问题化 【A】

> Ma, Yan, Shi, Kam, Wang, Liu, Chen, Zhang, Wong.
> *Can LLM Agents Stick to the Script? A Benchmark for Long-Horizon Consistency in Interactive Narratives.*
> **ICML 2026**, arXiv:2608.08160.

**他们做了什么。** 把「长程逻辑一致性」形式化为 **Narrative Commitment Preservation（NCP，
叙事承诺保存）**，在交互式叙事上做测试床。构建 NCP-Bench：100 个叙事环境（源自电影梗概），
每个环境含**结构化叙事规格（trajectory / commitments / initial facts）**，
在玩家 agent 与叙述者 agent 的交互过程中**自动检查**。

**关键结果（原文数字）：**

- 「高语言质量并不保证承诺保存」（*high linguistic quality does not guarantee commitment preservation*）
- **最佳模型 GPT-5.2，20 轮后存活率仅 42%**
- 各模型**事实冲突率 40%–68%**
- 100 轮上限内，**只有零星几轮**满足全部成就承诺

**对 Loom 的意义 —— 逐条对照：**

| NCP-Bench 的构造 | Loom 的对应物 | 状态 |
|---|---|---|
| trajectory | L1 情节层（POCL 因果） | 已有 |
| commitments | **L3 作者承诺层** | 已有，且是 Loom 的差异化核心 |
| initial facts | L2 设定 / lore | 已有 |
| 自动检查 | 28 个门禁校验器 | 已有 |

**结论：Loom 的三层 IR 与 NCP-Bench 的问题建模是同构的。** 这不是巧合——两者都源自
Riedl & Young 的 IPOCL 传统。但它给了 Loom 一件此前没有的东西：**一个外部可引用的数字**，
说明「承诺保存」是真实的、被测量的、连最强模型都做不好的问题。

**可直接用于对外表述（不夸大版）：**
> 业界 benchmark 显示，即使最强的前沿模型，在 20 轮交互后也只能维持 42% 的叙事承诺存活率。
> Loom 把「承诺」提升为 IR 的一等公民，因此它是**可以被校验的**，而不是靠提示词祈祷。

⚠️ **不要写的版本**：「Loom 解决了承诺保存问题」。论文没证明任何系统能解决，
Loom 也没有在 NCP-Bench 上跑过分。**只宣称「可校验」，不宣称「更好」。**

---

### 2.2 ConStory-Bench：一致性错误的密度、位置与预警信号 【A】

> Li, Guo, Wu, Lee, Li, Xie（Microsoft Research / SUTD）.
> *Lost in Stories: Consistency Bugs in Long Story Generation by LLMs.*
> arXiv:2603.05890.

**规模。** 2,000 个 prompt，四种任务场景（Generation / Continuation / Expansion / Completion），
输出目标 8,000–10,000 词。配套 ConStory-Checker：**5 大类 19 子类**错误分类，
三阶段证据链（抽取 → 配对 → 证据链 + 字符级 offset）。

**指标：** CED（每 10K 词的一致性错误数，越低越好）+ GRR（分组相对排名）。

**三个对 Loom 有直接工程价值的发现：**

**(1) 错误随长度近似线性增长，但模型长度偏好差异极大。**
GPT-5-Reasoning CED 0.113；DeepSeek-V3.2-Exp 0.541；MiniMax-M1-80k 3.447。
**同一个任务，最好的和最差的差 30 倍。** → Loom 的 `MockGenerator` 之外，
真实模型选型会显著影响结构健康分。**健康分是「模型 + 提示词 + IR」的联合函数，
不是 IR 的纯函数。** 这一点目前文档没有说清楚，建议在 README 明确。

**(2) 错误位置不随机 —— 事实在前，矛盾在后，集中在 40–60%。**

| 错误子类型 | 事实提及位置 | 矛盾出现位置 | 间距 |
|---|---|---|---|
| 绝对时间矛盾 | 22.6% | 48.9% | 29.7% |
| 核心规则违反 | 23.7% | 39.4% | 23.4% |
| 地理矛盾 | 20.4% | 39.2% | **31.0%** |
| 视角混乱 | 13.7% | 12.2% | **4.7%** |

→ **一致性校验的收益集中在中后段（40–60%），前 30% 几乎检查不出东西。**
这对 Loom 有两个含义：
- （可选优化）对长篇可以做**分段校验**，前段降频。但这是性能问题，不是正确性问题，
  **Loom 当前规模不需要做**——记录，不实施。
- （更重要的是）**这解释了为什么「写完再校验」是必要的**。矛盾与它的事实源头平均间隔
  23–31% 篇长，意味着**任何局部窗口都看不见它**。这是全篇校验存在的理由，可以写进设计文档。

**(3) 一致性错误集中在高不确定性区域。**

| 指标 | 全文基线 | 错误内容 | 相对差异 |
|---|---|---|---|
| 熵（bits） | 1.1438 | 1.2814 | **+12.03%**（Qwen3-30B） |
| token 概率 | 0.6895 | 0.6522 | **−5.41%** |
| 困惑度 | 1.8875 | 1.9354 | +2.54% |

→ **存在可用于预警的信号，但差异只有 5%–19%。**
**判断：不建议 Loom 现在做「基于不确定性的预警」。** 理由：
- 需要 logprobs 访问权，而 Loom 的 `Generator` 抽象（含 `MockGenerator`）**没有这个接口**，
  加了会破坏「无 API key 也能端到端跑通」这条铁律（铁律 6）。
- 信号强度 5%–19% 意味着单独使用几乎无判别力，只可能作为特征之一。
- **记录为「有外部证据支持，但工程代价大于收益，暂不做」。**

**Loom 已覆盖的部分（对照 19 子类，避免重复造轮子）：**
Loom 已有因果成环检查（`structure.py`：*「因果关系不能成环（除非是刻意的循环叙事，
需在承诺层登记）」*）与孤儿节点检查（`ir.plot.orphans()`），对应 NCP 的
「因果理性 KC（保持 DAG）」与「叙事完备性 KN（开头可达、通向结尾）」。
**这两项是 Loom 已有的，不需要新增。**

---

### 2.3 PLOTTER：图规划 vs 文本规划 —— Loom 架构赌注的外部验证 【A】

> *Planning Beyond Text: Graph-based Reasoning for Complex Narrative Generation.*
> **ACL 2026 Findings**, arXiv:2604.21253.

**核心主张。** 直接在文本表示上做叙事规划本质低效——缺乏对情节依赖的显式建模。
改用**双图表示**：事件图（边带 `Causal / Foreshadowing / Suspense` 标签）+ 角色图
（节点含核心性格、内部冲突、外部目标、隐藏秘密；边含冲突/合作/情感/隐秘）。
多 agent 评审团（Theme → Character → Plot）在图上跑 Evaluate-Plan-Revise，
编辑器执行原子操作，且**每个操作都要过两道纯符号、不依赖 LLM 的约束**：
KC（因果子图保持 DAG）、KN（所有节点从开头可达且有路径通向结尾）。

**这正是 Loom 铁律 1（IR 是产品本体，文本只是视图）与铁律 5（提示词是软的，代码是硬的）
的学术版本。** 「确定性符号约束兜住 LLM 评审的不可靠」≈ 铁律 5 的原文。

**结果（GPT-4.1 主干，成对比较胜率）：**

| 维度 | vs LLM-Plan-Write | vs Dramatron | vs DOC |
|---|---|---|---|
| Narrative | 72% | 74% | **92%** |
| Thematic | **100%** | 90% | 86% |
| Characterization | **100%** | 76% | 92% |
| Dramatic Engagement | 96% | 72% | 92% |
| **Premise Fidelity** | **40%** | **14%** | **44%** |

**⚠️ 前提忠实度是明显短板 —— 图的迭代精炼会偏离原初前提。**
这是本轮对 Loom **最有操作价值**的一条发现。它给了 `CriticLoop` 的
「只自动修文风、结构类只提提案」边界一个**独立于「保护作者意图」的第二条理由**：

> 自动改结构会漂移。不是道德上不该做，是**经验上做不好**——
> 图规划领域最强的系统在这个维度上也只有 40%。

**因此建议（待作者决策）：把「前提忠实度」做成可测量的门禁项。**
不是靠提示词约束（铁律 5：能在代码里兜底的事，不求模型配合），而是：
在 IR 里显式存 `premise`（一句话想法），加一个校验器比较
「结局 / 关键转折是否仍与 premise 的承诺一致」。
**这会让 L3 承诺层从「登记处」变成「有牙齿的约束」。**

**第二个可直接采用的数字 —— 迭代次数：**

| 配置 | 结果 |
|---|---|
| K=3（默认） | Distinct-2 = 0.793，Self-BLEU = 0.017（最佳平衡点） |
| **K=5** | **质量下降**（Distinct-2 = 0.640），编辑成功率降至 0.83 |

Loom 当前 `CriticLoop.max_rounds = 2`（已核实：`loom/pipeline/engines.py:1043`）。
PLOTTER 的证据说：**更多迭代不是更好，K=3 是峰值，K=5 反而退化。**

**判断：不建议盲目把 2 提到 3，但可以把它变成一个被测过的决定。**
理由：PLOTTER 的 K 是「图编辑轮」，Loom 的 `max_rounds` 是「文风修订轮」，
不完全等价。但「迭代会退化」这个现象值得 Loom 自己做一次小实验
（同一 IR，max_rounds ∈ {1,2,3,4}，看健康分与文本多样性）。
**成本极低（改一个参数跑 4 次），且能把一个拍脑袋的默认值变成有依据的默认值。**

**其他可借鉴点（低成本、高收益）：**
- **跨 agent 验证只放行获得一致支持的编辑** —— Loom 的 `CriticLoop` 是单 reviser。
  可参考其「一致性门」思路，但**不必**引入多 agent（成本 523k tokens/篇，$1.68/篇，
  对 Loom 的目标用户过重）。
- **消融显示三模块协同 >80%，远超单模块之和（+29% 故事线，+34% 剧本）** ——
  支持 Loom 已有的「多校验器并存」路线，**不要为了精简而砍校验器**。

**局限（须一并记录）：** 数据集仅 50 个前提，每类样本量小；高度依赖 LLM 主干能力
（在 DeepSeek-R1 上优势小于 GPT-4.1）；未与 StoryWriter 同条件对比。
→ **这些是论文自己的声明，不是我的判断。引用时不要省略。**

---

### 2.4 Reactive Writing：作者察觉不到 AI 的影响 【A】

> Bhat, Aubin Le Quéré, Naaman, Jakesch.
> *Reactive Writers: How Co-Writing with AI Changes How We Engage with Ideas.*
> **CHI 2026**, arXiv:2603.10374.

**方法。** 混合方法：19 人回溯式访谈 + 对 **1,291 次 AI 协同写作会话**的量化追踪。

**核心发现（原文）：**

- 与 AI 建议打交道（阅读 + 决定接受/拒绝）**取代了**构思和语言生成，
  成为写作的**中心活动**。
- 作者常常**没有先完成自己的构思**，因此 AI 建议的想法为后续方向**下了种子**，
  作者再去铺陈这些方向。
- **作者没有察觉 AI 的影响，并且感觉完全掌控着自己的写作——因为在原则上，
  他们随时可以编辑最终文本。**
- 该研究命名为 **Reactive Writing**：「一种**评估优先、建议主导**的写作实践，
  它显著区别于 AI 辅助下的传统写作，并且**极易受到 AI 诱导的偏见与观点漂移的影响**。」

**这条对 Loom 的意义超过其他三条 —— 因为它同时是威胁和机会。**

**威胁：** Loom 的 Proposer 机制（生成提案，作者决定接受/拒绝）
**正是论文描述的那种交互**。如果设计不当，Loom 会把用户推进 Reactive Writing：
作者不再先构思，而是从提案里挑。这恰恰是 Loom 声称要反对的东西。

**机会（且已被 Loom 部分实现）：** 论文说的缺失物是**觉察（awareness）**。
Loom 的 `provenance/process.py` 已经在记录「人类做了哪些判断」。

**⚠️ 但有一个关键缺口 —— 这是本轮我最想说的一点：**

> Loom 的 judgement 记录目前**只在申诉时才导出**（`cmd_process_report`）。
> 论文说的缺失是**写作过程中的觉察**，不是**事后申诉时的证据**。
> 一个在写完三天后才看到「你拒绝了 7 次提案」的作者，**他的写作过程并没有被改变**。

**建议（具体、低成本）：在 `CriticLoop` 与 Proposer 的交互中实时回显判断计数。**
例如每次作者做出接受/拒绝时，输出一行：

```
已做判断 12 次（接受 5 / 拒绝 7）— 本场 AI 起草 3 段，你改写了 2 段
```

这不是新功能，`CreativeProcessReport` 的数据**已经全部存在**，只是当前只在
`build_process_report()` 时才聚合。**把它提到写作时，是把已有能力换个出口。**

同时，这给了决策 #2（观众模拟作为卖点）一个更硬的措辞：
> Loom 不预测爆款。它做的是**让你看见 AI 正在把你往哪儿带**——
> 这恰恰是 CHI 2026 证明作者独自做不到的事。

---

## 3. 产品与留存基准

### 3.1 a16z：把留存从 M0 重定基到 M3 【B】

> Rodriguez & Immerman, *Retention Is All You Need*, a16z, 2025-09-10.

**核心论点。** 分析了数百家 AI 公司后，建议**把留存曲线从 M0 重定基到 M3**，
因为「AI 游客（AI tourists）」涌入——大量用户在 M0 试用后立刻流失，
让 M0 起的曲线失去意义。

**具体建议：**
- 队列拆分为：获客（M0–M3）、留存（M3–6–9）、扩张（M9+）。
- 曲线**通常在 M3 附近走平**。
- 新指标 **M12/M3**（而非 M12/M0）。
- 关注**「微笑型」留存曲线**：已流失/低活用户在能力变强后回来。ChatGPT 是范例。
- 追踪 **M3 时的单留存客户成本**。
- **原文警告：**「如果你的留存曲线在 M3 之后仍显著下滑，和/或根本没走平，
  这是一个警告信号——**最初的用户惊喜没有转化为持久的实用价值**。」

**对 Loom 的直接推论（这是本轮对商业侧最强的一条）：**

| a16z 的观察 | 对 Loom 的含义 |
|---|---|
| M0 曲线被「游客」污染 | **不要用「生成了多少字」当北极星**——它测的是游客 |
| 曲线应在 M3 走平 | Loom 的持久价值必须是**跨作品**的：作者的第二部、第三部 |
| 追踪 M3 单留存客户成本 | Loom 的成本主要是 LLM 调用；**长篇重度用户可能成本倒挂** |
| 「微笑曲线」 | 能力变强会让老用户回来 → **IR 是资产**：旧 IR 在新校验器下重新体检，就是召回钩子 |

**最后一行是 Loom 一个尚未被说出来的优势：** 因为 IR 是持久资产而不是一次性文本，
Loom 天然具备「微笑曲线」的触发机制——**发布新校验器时，老用户的旧 IR 会自动获得新的
体检结果**。这不需要新功能，只需要在发布节奏上刻意利用。
**建议记录为增长机制，不是产品功能。**

---

### 3.2 DataEye / 澎湃：98.7% 未回本 —— **第一轮已知，本轮补充新细节** 【A】

> **更正（自查后）：** 98.7% / 0.48% 这组数字**在第一轮 `外部调研_2026-09.md`
> （§2.1，第 115–152 行）已经记录并用于决策 #1**。本轮**不是新发现**。
> 本轮真正新增的是三块：**(a) 供需剪刀差**、**(b) 抖音 8/3 已上线预检工具**、
> **(c) 红果治理的具体口径与量级**。下面的表保留完整数字仅为自包含，
> 但**引用时请以第一轮为准**，本轮的增量在表后。

> 澎湃新闻，2026-09-01。数据来源：**DataEye-ADX 行业版**，
> 统计区间 **2026-01-01 至 2026-06-30**。

**核心数字（第一轮已有）：**

| 指标 | 数值 |
|---|---|
| 抖音端原生 AI 剧漫剧新剧 | **22.19 万部** |
| 累计播放 | 5157.38 亿次 |
| 全年市场规模预计 | > 400 亿元 |
| AI 剧漫剧用户 | 1.2 亿（2025）→ **6 亿**（2026 H1） |
| 微短剧总用户 | 8.51 亿 |
| **破亿作品** | **1055 部 / 22.19 万 = 0.48%** |
| **半年内未回本** | **98.7%（每 77 部仅 1 部回本）** |
| AI 漫剧中档制作成本 | 800–1200 元/分钟（真人的 1/5） |
| 红果 | MAU 3.57 亿，日均使用 125 分钟 |

**「AI 降低的不是成本，而是赔钱的门槛。」**（DataEye 副总裁林启文，原文引用）

**⚡ 本轮增量 (a)：供需剪刀差（最有信息量的一组，第一轮未记录）**

| | 数量占比 | 播放占比 | 趋势 |
|---|---|---|---|
| AI 仿真人剧 | Q2 **70.62%**（冲高） | 4–5 月 80% → 6 月 **68%**（回落） | ❌ 剪刀差 |
| 3D 漫 | 稳定 ~20% | 3 月 6.71% → 6 月 **29.48%**（**16 倍播放增长**） | ✅ 逆势 |

→ **产量与播放不成正比。少而精能穿透同质化。** 3D 漫《发配边关，罪妻开荒养出战神》
以 **21.27 亿**播放断层领先。

**对 Loom 的战略含义（1 来自第一轮，2/3 为本轮增量）：**

1. **不要按「生成量」卖。**（第一轮已确立）按生成量收费 = 激励用户生产更多那 98.7%。
   Loom 的价值主张应锚在**「避免浪费」**：在投入制作之前就知道这部结构上有问题。
2. **⚡ 增量 (b)：`pre_submit_check` 不是创新，是入场券。** 抖音短剧创作者中心
   **已于 8 月 3 日上线预检工具**（面向 AI 剧版权方与导演机构，成片上线前筛查
   肖像权、版权风险）。**市场需求已被验证，但已有玩家。** Loom 的差异必须在于
   **叙事结构 + 创作过程证据**，不在版权/肖像权——后者 Loom 做不了也不该做。
3. **⚡ 增量 (c)：红果的治理口径是 Loom 合规检测器的真实对标物：**
   「高频 AI 脸、同质化及素材违规治理专项」；对「高频复用」剧集采取**零流量**措施；
   4 月一周内拦截下架 **3522 部**低质漫剧；截至 4 月底处置**超万部**低质 AI 剧
   （画面粗糙、剧情混乱、低俗猎奇）。
   → Loom 本轮新增的三个检测器（burstiness / adverb / dialogue rhythm）
   瞄准的正是「同质化」这个已**被平台实际处罚**的品类。
   **这是它们存在的商业理由，比「文风好」强得多。建议在文档里改用这个理由。**

---

## 4. 合规：投资额分类分层 —— 知识层有，代码层没有

> **更正（自查后）：** 我最初的判断是「Loom 只建模了两层合规」。**这个说法不准确。**
> 项目记忆 `MEMORY.md` 的合规表**已经记录了《微短剧发展管理办法》第 17 条的投资额三类监管**
> （≥80 万 / 30–80 万 / <30 万）。所以这不是「未知」。
> 但核实代码后确认：**`loom/audit/selfcheck.py` 的 `DIMENSIONS`（14 行）里没有任何一条
> 对应投资额/审核路径**（已 grep `投资额|三类|省级|总局|分类分层`，在 `loom/` 下
> 只命中 4 处，全是第 34 条标识义务与平台规则，**无投资额**）。
>
> **准确表述：这是「知识在文档里、不在代码里」，不是「知识缺失」。**
> 差一个层级，但性质不同——前者是补一条维度，后者是新发现。按前者处理。

| 层 | 文件 | 生效 | 文档层 | 代码层 |
|---|---|---|---|---|
| **源** —— 标识义务 | 《人工智能生成合成内容标识办法》（国信办通字〔2025〕2 号） | 2025-09-01 | ✅ | ✅ |
| **流** —— 落到微短剧 | 《微短剧发展管理办法》第 34 条 | 2026-09-01 | ✅ | ✅ |
| **分类分层** | 《管理提示（AI 微短剧分类分层标准）》 | **2026-07-01** | ✅（记作第 17 条） | ❌ **无** |

**第三层的具体规则（按投资额分三类监管，两条红线 30 万 / 80 万）：**

| 类别 | 判据 | 审核方 |
|---|---|---|
| 一类 | 投资额 **≥ 80 万元**，或特殊题材 | **总局发证** |
| 二类 | 投资额 **30–80 万元**，一般题材 | 省级广电部门审核 |
| 三类 | 投资额 **< 30 万元** | **平台审核** |

配套：《微短剧发展管理办法》**第 17 条**——第三类由符合条件的播出单位履行内容管理职责，
**播前审核并标注节目编号**。

**证据强度说明：** 该分类分层标准由**两个独立来源交叉印证**
（澎湃 2026-09-01 报道 + 一份 2026-06-26 的行业解读），两处对 30 万 / 80 万两条红线的
描述一致。**但我未读到原文**，故标为 **B 级（软）**，不写入 README 的合规章节，
仅在此登记并建议核实。

**为什么这一层对 Loom 有实际意义（不是凑数）：**

> 它决定了用户要走哪条审核路径，而 Loom **目前完全不建模制作预算**
> （已核实：`loom/` 全库 grep `budget|invest|投资|预算` 只命中
> token 预算与 `thread_budget`，无投资额概念）。

**建议在 `selfcheck.py` 的 C 层（作者自行确认）加一条：**
「你的制作投资额属于哪一档？这决定审核路径（总局 / 省级 / 平台）。Loom 不估算投资额。」
—— 放进 C 层而非 A 层，因为它是**用户输入**，不是 Loom 可机检的事实。
这符合 `selfcheck` 已有的设计原则：**MANUAL 是第三态，不并入 ok**。

**另外两条值得记录的条款（来自澎湃对《办法》的引述）：**
- **第 33 条**：播出前应按规审核并同步报主管部门；对传播微短剧的**重点账号**建立
  **信用评价体系**并实施重点管理。
- **第 37 条**：播出单位应定期审核、评估、验证**算法机制机理、模型、数据和应用结果**，
  **优先推荐优质微短剧**，不得使用诱导用户沉迷、过度消费的算法模型。
- **第 38 条**：建立**分级管理制度和退出机制**。

→ 第 37 条尤其值得注意：它把「优质」写成了平台的**法定义务**。
这为「结构健康分」这类质量信号创造了一个**合规驱动的市场**，而不只是创作者自嗨。
**这可能是 Loom 观众模拟 / 健康分最硬的一条商业论据**——但它是 B 级推论，
是我在原文条款基础上做的推断，不是原文主张。**记录为假设，不作为结论。**

---

## 5. 给 Loom 的建议（按行动优先级）

### 5.1 应立即做（低成本、有外部依据、不触碰作者保留决策）

| # | 建议 | 依据 | 成本 |
|---|---|---|---|
| 1 | **写作时实时回显判断计数**（接受/拒绝/改写次数），而非只在申诉时导出 | CHI 2026 Reactive Writing | 极低——数据已在 `CreativeProcessReport` 中，只换出口 |
| 2 | **把 `pre_submit_check` 的存在理由从「合规」改写为「避免成为那 98.7%」** | DataEye 98.7% / 0.48% | 极低——文档改动 |
| 3 | **三个合规检测器的文档理由改为「平台已实际处罚同质化」** | 红果零流量 / 处置超万部 | 极低——docstring 改动 |
| 4 | **在 `selfcheck` C 层加一条「投资额档位 → 审核路径」**（知识已在 MEMORY.md，缺的是代码维度） | 双源交叉 + 代码核实 | 低——C 层加 1 条，不触碰 A 层 |
| 5 | **跑一次 `max_rounds ∈ {1,2,3,4}` 的小实验** | PLOTTER K=3 峰值、K=5 退化 | 低——改一个参数跑 4 次 |

### 5.2 应提交作者决策（我不单方面改）

| # | 决策点 | 我的倾向 |
|---|---|---|
| A | 是否新增「前提忠实度」校验器（把 premise 存进 IR 并校验结局/转折是否仍一致）？ | **倾向做。** PLOTTER 40%/14%/44% 说明这是图规划路线的公认短板，而 L3 承诺层正好是防漂移的正确位置。它让 L3 从「登记处」变成「有牙齿的约束」。 |
| B | 是否做「基于 token 不确定性的错误预警」？ | **倾向不做。** 信号只有 5%–19%；且需 logprobs 接口，会破坏铁律 6（无 API key 可跑通）。记录为「有依据但代价大于收益」。 |
| C | 是否按长度分段校验（前段降频）？ | **倾向不做。** 依据（错误集中 40–60%）成立，但 Loom 当前规模无性能压力。属于过早优化。 |
| **D** | **卖点措辞：是「预测爆款」还是「避免浪费」？** | **倾向后者，且这不是推翻决策 #2 而是给它一个更好的落点。** 决策 #2 已判定观众模拟不当卖点（LLM 评委与人类反相关）——**那个判断仍然成立**。但 98.7% / 0.48% / 剪刀差这组数据说明真正的可售价值是「在投入制作前就知道不值得做」。**「避免浪费」不要求预测成功，只要求结构性淘汰**——这恰好避开了 LLM-as-judge 失效的那个位置。 |

### 5.3 仅供记录（不改变任何东西）

- Loom 已有 DAG 检查与 orphan 检查，对应 NCP 的 KC / KN 约束 —— **不需要新增**。
- ConStory 的 19 子类错误分类可作为**未来校验器命名的参考坐标系**。
- 健康分是「模型 + 提示词 + IR」的联合函数，不是 IR 的纯函数 ——
  **建议在 README 明确**，否则用户会误读分数。

---

## 6. 我剔除了什么（诚实声明）

以下内容**被检索到但判定为 C 级（勿引）**，未进入任何结论：

- 「2026 年 N 款 AI 写作工具横评」类榜单文（≥6 篇）——无方法论、疑似 SEO/联盟营销内容，
  且**与 Loom 的定位（IR 编译器）不同类**，比较无意义。
- 「AI Interactive Storytelling: 20 Advances (2026)」类汇编站 —— 无一手来源。
- X 平台从业者讨论 —— **本轮未能有效检索**。X 内容不在可抓取范围内，
  检索返回的均是被转载到营销站的第二手内容。**这是一个真实的检索缺口，
  不是「没有值得看的讨论」。**

**已知检索缺口：**
1. **X / 从业者一手讨论未覆盖**（抓取受限）。
2. ACM DL 与 SIGCHI 程序页均在 Cloudflare 机器人保护之后，
   Reactive Writing 是通过 **arXiv 预印本**取得原文的；
   **若正式版与预印本有出入，以正式版为准。**
3. 《管理提示（AI 微短剧分类分层标准）》**原文未读到**，仅双源交叉，标 B 级。
4. GitHub 生态本轮**未重新遍历**——第一轮 `外部调研_2026-09.md` 已覆盖，
   且项目已锁定「借方法论、不装」的路线（AGPL 红线），无重复检索必要。

---

## 7. 一句话总结

> **学术界刚刚追上了 Loom 的架构赌注（ICML 2026 把「承诺保存」问题化），
> 也刚刚指出了这条路线公认的短板（前提漂移，PLOTTER 40%）；
> 市场数据（第一轮已知，本轮用剪刀差补强）印证的仍是「产能不是瓶颈、筛选才是」；
> 而 CHI 2026 证明了 Loom 已经在采集、却只在申诉时才用的那份数据
> ——「你做了哪些判断」——恰恰是作者独自看不见的东西。**
>
> **最高杠杆的一件事，是把那份数据从「申诉材料」搬到「写作时」。**

---

## 8. 本轮相对第一轮的增量清单（自查结论）

为避免把重发现当新发现，逐条标注：

| 内容 | 状态 |
|---|---|
| NCP-Bench / 承诺保存（ICML 2026） | ✅ **全新** |
| ConStory-Bench（CED / 位置分布 / 不确定性） | ✅ **全新** |
| PLOTTER（图规划 / 前提忠实度 40% / K=3） | ✅ **全新** |
| Reactive Writing（CHI 2026） | ✅ **全新** |
| a16z 留存重定基 M3 | ✅ **全新** |
| 98.7% / 0.48% / DataEye 市场数据 | ♻️ **第一轮已有**（§2.1）；本轮增量 = 剪刀差 + 抖音预检工具 + 红果治理量级 |
| 投资额分类分层（30/80 万） | ♻️ **知识层已有**（MEMORY.md 记作第 17 条）；本轮增量 = 确认**代码层 `selfcheck` 无此维度** |

**教训（值得记进项目规则）：** 写调研报告前先 grep 已有的调研文档与项目记忆，
否则会把第一轮的结论当作本轮发现再交一次。**红笔自检查出两处，均已更正。**

---

*生成时间：2026-09-14 · 证据等级：A 级 4 源、B 级 2 源 · C 级已剔除并在第 6 节登记*
