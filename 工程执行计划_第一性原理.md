# Loom 工程执行计划（第一性原理推导）

> 推导自 `外部调研_2026-09.md` + `外部调研_2026-09-14_第二轮.md`。
> 方法：先列**不可约事实**，再推导，最后才落到做什么。不做需求清单式罗列。
> 日期：2026-09-15

---

## 0. 第一原理：一句话

> **当文本的生产成本趋近于零，稀缺的不再是故事，而是「关于这个故事的信息」。**
>
> **Loom 的产品是信息，不是故事。**

这一句是全部推导的起点。下面是它的来历和后果。

**为什么这是不可约的？**
一个故事本质上是**承诺装置**：作者向读者承诺某些东西会重要，然后必须兑现（= L3）。
写作是**对未来文本的不确定决策**：你早早承诺，之后被约束。
当文本免费（F1），唯一稀缺的只剩两件事：
- **(a) 我的承诺还成立吗？** → **校验**
- **(b) 哪些是我决定的，哪些是 AI 建议的？** → **觉察**

两者都是**关于制品的信息**，不是制品本身。

---

## 1. 不可约事实（F1–F7）

| # | 事实 | 源 | 等级 |
|---|---|---|---|
| **F1** | 生产成本→0（AI 漫剧 800–1200 元/分钟，真人 1/5；H1 22.19 万部）。价值全部迁移到**筛选**：破亿率 **0.48%**、**98.7% 半年未回本** | DataEye-ADX，2026 H1 | A |
| **F2** | 作者**察觉不到** AI 的影响，却**感觉完全掌控**（因为原则上随时可编辑）。命名 *Reactive Writing*：「评估优先、建议主导，极易受 AI 诱导的偏见与观点漂移影响」 | CHI 2026，19 人访谈 + **1291 次会话** | A |
| **F3** | 承诺保存是**真实的、已测量的、未解决的**失败：最强模型 GPT-5.2 在 20 轮后**存活率仅 42%**，事实冲突率 40–68%。**高语言质量 ≠ 承诺保存** | ICML 2026, NCP-Bench | A |
| **F4** | 图规划在 Narrative/Theme/Character/Drama 胜率 72–100%，**唯独前提忠实度 40%/14%/44%**。且 **K=3 峰值、K=5 退化**。迭代精炼**会**偏离原初前提 | ACL 2026, PLOTTER | A |
| **F5** | 留存须**重定基到 M3**（AI 游客污染 M0）。曲线应在 M3 走平；「微笑曲线」来自能力变强后老用户回归 | a16z, 2025-09 | B |
| **F6** | 网文三平台**一致禁止 AI 直出正文**（起点 >10% 即处理）；微短剧**只要求加标识**（第 34 条）。**同一 Loom：做网文=违规工具，做剧本=合规工具** | 起点/番茄/晋江 2026 口径 + 总局令 16 号 | A |
| **F7** | 门槛决定采用：InkOS（10 agent、37 维审计）在中文横评**只排第 8**，原因是本地部署 + 命令行 + 门槛高。Loom 比它更远 | 中文横评 | B |

---

## 2. 五条推导

### D1 · IR 不是实现细节，是**信誉机制**
「这个故事结构上是成立的」这句话要有人信，就必须**可验算**，不能是 LLM 猜的。
PLOTTER 的「两道纯符号约束（DAG / 可达性）兜住 LLM 评审的不可靠」就是这个原理的学术版。
→ **IR 的存在理由被外部独立证实了。** 铁律 1 成立。

### D2 · 判断必须对准**已测量的失败模式**，否则是装饰
Loom 已有 28 个门禁 + 7 个报表项。但对照 F1–F7，缺口排序是：

| 失败模式 | 证据 | Loom 现状 | 缺口 |
|---|---|---|---|
| 前提漂移 | F4（40%） | `premise` 存在（`models.py:282`）但**无任何校验器引用它** | 🔴 **最大** |
| 承诺保存 | F3（42%） | L3 存在，但**只登记不校验** | 🟠 中 |
| 觉察缺失 | F2 | judgement 已采集，但**只在申诉时导出** | 🟠 中（杠杆最高） |
| 一致性 | ConStory | 有校验器，且已有 DAG / orphan 检查 | 🟢 已有 |

### D3 · 可达性是**乘数**，不是加法
一个到不了用户手里的判断，价值为零。
**Loom 目前没有任何「能直接看的产物」** —— 5 个渲染器（fountain/ink/renpy/storyboard/text）
**全是文本格式，无 HTML**。28 个校验器的结果只活在终端里。
→ 35 个校验器如果没人看，等于 0 个。

### D4 · 持久价值来自**跨作品**，不来自单次惊艳
F5 说 M3 之后曲线要走平。对写作工具，「持久」= 你的第二部、第三部还用它。
Loom 已把 IR 落盘（`ir.json`），但**没有「用新校验器重跑旧 IR」的命令**。
→ 这正是 a16z 说的「微笑曲线」触发机制：发布新校验器时，老用户的旧 IR 自动获得新结果。
**这是增长机制，不是产品功能 —— 但它需要一个入口才存在。**

### D5 · 只做**结构性淘汰**，不做成功预测
F1 说真正的价值是「投入制作前就知道不值得做」。
且决策 #2 已判定 LLM 评委与人类判断**反相关**（把零样本 AI 故事排在《纽约客》之上）。
→ **「避免浪费」只要求结构性淘汰，不要求预测成功** —— 恰好避开 LLM-as-judge 失效的位置。
→ 推论：**不要按生成量定价**（那是激励用户生产更多那 98.7%）。

---

## 3. 四条执行原则

1. **先补判断的正确性，再补判断的广度。** 新增校验器必须对准 F1–F7 中已测量的失败模式。
2. **把已有能力换个出口，优先于新造能力。** judgement 数据已在 `CreativeProcessReport` 里了。
3. **可达性是乘数。** 一个能双击打开的产物 > 三个新校验器。
4. **不宣称做不到的事。** 不预测爆款；合规检测器不承诺降低误判率（结构性限制）。

---

## 4. 执行计划

> 排序依据：`(价值 × 置信度) / 成本`。P = Priority。

### P0 · 可达性（乘数项）

| # | 事项 | 为什么（推导） | 验收 |
|---|---|---|---|
| **P0.1** | **新增 HTML 渲染器 `loom/render/html.py`**（单文件、无外部依赖、双击可看） | D3。让 28 个校验器第一次被人看见 | 输出单文件 HTML；含健康分、三层 IR、校验明细、提案、判断痕迹 |
| **P0.2** | `cli write` 默认产出 `report.html` 并打印路径 | D3 | 一条命令后用户知道该双击哪个文件 |
| **P0.3** | HTML 报告**可分享**（自包含，无 CDN / 无外链） | F5（M0–M3 获客靠分享） | 断网可正常打开 |

### P1 · 觉察（杠杆最高，数据已有）

| # | 事项 | 为什么 | 验收 |
|---|---|---|---|
| **P1.1** | **写作时实时回显判断计数**（接受/拒绝/改写） | F2 + 原则 2。论文说的缺失物是**过程中的觉察**，不是**申诉时的证据**。三天后才看到「你拒绝了 7 次」，写作过程并没有被改变 | `CriticLoop` / Proposer 交互中输出一行计数 |
| **P1.2** | HTML 报告里显示「人类判断痕迹」区块 | D3 × F2 | 报告中有独立区块，标明数据来源 |

### P2 · 正确性（学界公认短板）

| # | 事项 | 为什么 | 验收 |
|---|---|---|---|
| **P2.1** | **新增「前提忠实度」校验器 `premise_fidelity`** | F4 + D2 最大缺口。`premise` 已在 IR 里但无人引用 → L3 从「登记处」变成「有牙齿的约束」 | 按铁律 15 交齐三样：`REQUIRES` 声明 + 反例 fixture + 文档计数 |
| **P2.2** | **`max_rounds ∈ {1,2,3,4}` 实测** | F4（K=3 峰值、K=5 退化）。把拍脑袋的默认值变成有依据的默认值 | 表格化结果写进文档；不盲改 |

### P3 · 修漂移

| # | 事项 | 为什么 | 验收 |
|---|---|---|---|
| **P3.1** | `pipeline_demo.py` 改用 `add_advisory()`，与 `cmd_audit` 一致 | **这是漂移不是决策**（自我更正）。先例已存在，跟随先例=铁律 19 | 两处同 IR 同分；**同时打出「结构分」与「工艺提示数」** |
| **P3.2** | 老报表项（`mirror_bookends`/`revelation_regression`/`thread_budget`）迁 `ADVISORY` | D1：一个主张只能有一个含义。健康分必须只表示「结构」 | 全部文档基线同步更新 |

### P4 · 持久性

| # | 事项 | 为什么 | 验收 |
|---|---|---|---|
| **P4.1** | **新增 `cli recheck <ir.json>`**：用当前校验器重跑旧 IR | D4 + F5 微笑曲线钩子 | 旧 IR 能出新的体检结果 |
| **P4.2** | **网文正文策略闸门** | F6：同一 Loom 做网文=违规工具。做成**策略而非删除**（可逆） | novel 媒介默认不出正文，有显式覆盖开关且留痕 |

---

## 5. 不做的事（同等重要）

| 不做 | 理由 |
|---|---|
| **不做反检测** | 已是机检红线（`tests/test_redlines.py`）。技术上徒劳、伦理上站不住、商业上致命 |
| **不做基于 token 不确定性的预警** | 判别力仅 5–19%，且需 logprobs，破坏铁律 6（无 API key 可跑通） |
| **不按长度分段校验** | 依据成立（错误集中 40–60%）但当前无性能压力 = 过早优化 |
| **不预测爆款** | D5 + LLM-as-judge 与人类判断反相关 |
| **不引入多 agent 评审** | PLOTTER 成本 523k tokens/篇、$1.68/篇，对目标用户过重 |
| **不宣称合规检测能降低误判率** | 结构性限制：平台用 Loom 拿不到的信息判定 |

---

## 6. 验收标准（全量跑通才算完成）

```
scripts/verify.py                54+ 项全过（新增项后应更高）
全部 tests/test_*.py             全绿
51+ 模块导入冒烟                  0 失败
scripts/pipeline_demo.py         端到端通过
scripts/demo.py                  端到端通过
cli write → report.html          产出且自包含
新增校验器                        三样齐全（REQUIRES + 反例 + 文档计数）
文档计数漂移检查                  0 处
```

---

## 6.5 执行结果（2026-09-15 全量推进后实测）

| 项 | 状态 | 实测 |
|---|---|---|
| P1.1 觉察回显 | ✅ 完成 | `loom/provenance/awareness.py`；31 断言；**4 个变异全部变红** |
| P1.2 报告内觉察区块 | ✅ 完成 | HTML 报告「人类判断痕迹」区块 |
| P2.1 前提忠实度 | ✅ 完成 | `premise_fidelity`；基线 PASS（95→ 不变），反例 WARN（96→91） |
| P2.2 `max_rounds` 实测 | ⚠️ **未能测出有效结论** | 见下 |
| P0.1/P0.2 HTML 渲染器 | ✅ 完成 | `loom/render/html.py`；24 断言；**4 个变异全部变红**；自包含无外链 |
| P3.1 pipeline_demo 口径 | ✅ 完成 | 改用 `add_advisory`，同时打出「结构分 / 工艺提示数」 |
| P3.2 ADVISORY 迁移 | ✅ 完成 | 基线 95→**96**、94→**95**；新增 `ADVISORY_READOUT` 第三类 |
| P4.1 `recheck` | ✅ 完成 | 带「上次以来变了什么」的快照对比 |
| P4.2 网文正文闸门 | ✅ 完成 | `loom/policy.py`；`WEB_NOVEL` 默认挡，可显式放行 |

**自证：61/61 通过（原 54）· 54 模块导入 0 失败 · 两个端到端 demo 全过。**

### P2.2 为什么没测出来（诚实记录）

`max_rounds ∈ {1,2,3,4,5}` 跑了三轮，结果**完全一致**。查下去发现两个原因，
第二个是本轮最有价值的产出：

1. **干净基线上循环体无事可做**（这是**正确行为**）→ 提前退出，轮数不影响结果。
2. **`CriticLoop` 的自动修订分支是死代码**（这是**bug**）→ 见下。

即便修好 bug，离线也测不出结论：`MockGenerator.generate("revise", ...)`
返回 `{'prose': <原样>, 'changed': False}` —— 它是确定性桩件，**设计上就不改写**。
所以「K=5 是否退化」这个问题**必须接真 LLM 才能测**。**记录为待办，不编数字。**

### ⚠️ 本轮抓到的真实 bug：`CriticLoop` 自动修订从未生效

类 docstring 写着「文风类（slop）→ **自动**改」，但它从未执行过。两处原因：

1. `slop_targets` 从 `run_all()` 的结论里筛 `slop:` 前缀；
   而 slop 结论走的是 **advisory 通道**（`cmd_audit` 用 `add_advisory`），
   registry 里**根本没有** `slop:*` 校验器 → `slop_targets` **恒为空**。
2. 即便修好第 1 点，原实现在「没有结构问题」时**先 `break`** ——
   而结构干净恰恰是**最常见**的情况 → 仍然轮不到文风修订。

**修法**：slop 目标改用 `scan_ir(ir)` 直接扫；退出条件改为
「无结构问题 **且** 无待修文风问题」。
**回归测试**：`tests/test_criticloop.py`（8 断言），
把两处 bug 分别改回去都会**变红**（已实测）。

> 教训：`max_rounds` 实验的真正价值不是拿到一个数字，
> 而是**「1/2/3/4/5 轮结果完全一致」这个异常本身** ——
> 它指向一条从未被执行过的代码路径。**一个跑不出差异的实验也是实验结果。**

### `ADVISORY_READOUT`：为什么必须分出第三类

迁移老报表项时发现 `thread_budget` **归不进任何一类**：
它不是机会型（不是「你接下来可以写什么」），也不是缺陷型 ——
它是**无条件测量**（报「价值线分布：X×3（共 N 条）」），干净稿子上**必须**说话。
硬塞进缺陷型会逼出一个**不存在的故障**的反例 —— 为了分类整齐而造假。

故新增 `ADVISORY_READOUT`，判据是「必须在基线上说话」。
这与 `ADVISORY_DEFECT` 的诞生方式一致：**当一条非空转判据逼出错误结论时，就分集合。**

---

## 7. 与待决问题的关系

| 原决策 | 本计划的处理 |
|---|---|
| #1 媒介优先级 | **按 F6 执行剧本优先**，但做成**策略闸门**（P4.2）而非删除 → 可逆 |
| #2 观众模拟作卖点 | **不推翻**：仍不作卖点。改为「避免浪费」叙事的技术底座（D5） |
| #3 AI 占比 <10% | 架构上已满足（校验 > 生成）。不追数字 |
| #4 2.0 首发 Ink | 不变 |
| #5 健康分容纳非结构信号 | **P3.2 收敛**：老报表项迁 ADVISORY，健康分只表示结构 |
| #7 第一分钟可及性 | **升为 P0**（乘数项） |
| #9 pipeline_demo 口径 | **P3.1 修**（漂移，非决策） |
| #10 前提忠实度 | **P2.1 做** |
| #11 不确定性预警 | **不做**（§5） |
| #12 卖点措辞 | 采用「避免浪费」 |
| #13 max_rounds | **P2.2 实测** |
| #14 judgement 搬到写作时 | **P1.1 做**（最高杠杆） |
