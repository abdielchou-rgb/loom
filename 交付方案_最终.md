# 龙骨 Keel · 最终交付方案

> 日期：2026-09-19 ｜ 版本 1.0.0 ｜ 远端 https://github.com/abdielchou-rgb/keel
>
> **证据分级贯穿全文**：`实测` = 本机跑出来的数字；`推断` = 有依据但没直接测；
> `未验证` = 没测过，只是合理猜测。三者混在一起，方案就变成了装饰。
>
> **本文所有计数都是从代码现取的**（`registry_stats()` / `pkgutil` / `len(METHODS)`），
> 不是手抄的。这个项目的漂移检查器已经抓过两次手写数字漂移，所以这里不手写。

---

## 0. 一句话

> **龙骨 Keel 1.0 = 剧本 / 微短剧的「结构体检 + 合规自检」工具。**
> 形态：一条命令 → **0.5 秒**（`实测`）→ 一份自包含、断网可开、能直接发给别人的 HTML 报告。

它**不替你写**，也不夸你写得好。它用结构化证据指出你的故事哪里站不住、
用留痕保护你的权益，把每一个「改不改、怎么改」的决定交还给你。

卖点不是「写得更快」——那是加剧那 98.7%。是**避免浪费**：
在投入制作之前就知道这部结构上有问题。

---

## 1. 名字：龙骨 / Keel

### 为什么换掉 Loom（`实测`，2026-09-19 查过）

| 名字 | 判定 | 证据 |
|---|---|---|
| **Loom** | ❌ 不可用 | 已被 **Atlassian 收购**（2023-11 交割）；开发圈里 "Loom" 还指 **Java Project Loom**（虚拟线程） |
| **Fabula** | ❌ 更糟 | 同赛道直接占用：`fabula.productions` 做「剧本→知识图谱」，`fabulaos.io` 自称 "AI-native OS for story development" |
| **Textus** | ❌ 撞音 | 与 `TextUs`（短信营销软件）读音完全相同 |

**Fabula 本来是最自然的选择** —— fabula / syuzhet 是叙事学里 IR 与文本的原始对立，
正是本项目「IR 是本体、文本只是视图」的术语来源。但**越是精确的名字越可能已经被占**：
正因为贴切，同赛道已经挤满了。

### 为什么是龙骨

吊顶与船体里那层**看不见却承重**的结构。正对应本项目的立场：
决定一部作品站不站得住的，是**正文之下那层结构**。

- 中文母语可读、好记、未被同赛道占用
- 英文 **Keel** 是同一件事的另一个说法：先定龙骨，再定船
- 改名已完成（包名 / CLI / 仓库 / 全部文档），见 `CHANGELOG.md`

> `未验证`：**没有做商标检索**。上述只是「软件领域重名调研」，
> 不等于法律上的可用性判断。真要对公众发布前，这一步必须补。

---

## 2. 交付什么

| # | 交付物 | 形态 | 状态 |
|---|---|---|---|
| 1 | **可安装的 Python 包** | `pip install keel-narrative` → 得到 `keel` 命令 | `实测` 可装 |
| 2 | **44 个校验器**（35 门禁 + 9 报表项） | 每条带理论出处 + 反例 fixture | `实测` |
| 3 | **6 个渲染器** | text / fountain / storyboard / ink / renpy / html | `实测` |
| 4 | **13 个结构模板** | 好莱坞 beat sheet、起承转合、Reagan 六弧线**平级共存** | `实测` |
| 5 | **7 个 API 方法** | `keel api` 稳定 JSON 出口（插件契约） | `实测` |
| 6 | **自证工具** | `scripts/verify.py`：零依赖、不需要 key | `实测` 87·0 |
| 7 | **零配置启动器** | `scripts/keel.bat`（Windows）/ `keel.sh` | `实测` |
| 8 | **单文件包** | `keel.pyz`，不用装 Python 包也能跑 | `实测` |

### 三条差异化（每条都有外部证据，不是自夸）

| 差异化 | 外部证据 | Keel 的实现 |
|---|---|---|
| **承诺可校验** | NCP-Bench（ICML 2026）：最强模型 GPT-5.2 在 20 轮后**存活率仅 42%**，事实冲突率 40–68% | L3 承诺层是 IR 一等公民，因此**可验算**，不靠提示词祈祷 |
| **前提不漂移** | PLOTTER（ACL 2026）：图规划四维度胜率 72–100%，**唯独前提忠实度 40%/14%/44%** | `premise_fidelity` 校验器 |
| **觉察可见** | CHI 2026 *Reactive Writing*（19 人访谈 + 1,291 次会话）：作者**察觉不到** AI 的影响，却感觉完全掌控 | 写作时回显「已做判断 N 次（采纳 X / 驳回 Y）」 |

---

## 3. 现状盘点（`实测` 2026-09-19）

```
版本              1.0.0
校验器            44   （35 门禁 + 9 报表项）
咨询项 ADVISORY    9
结构模板          13
渲染器             6
模块              75
API 方法           7   （api_version = 1）
测试文件          35   →  35 / 35 全绿
门禁自证          87 · 0 失败
```

### P0–P6 真实状态（逐项对着代码核过，不并入「已完成」）

| 阶段 | 状态 | 落地物 |
|---|---|---|
| P0 可达性 | ✅ | `keel.bat` / `keel.sh` / `build_zipapp.py` / `check_launch.py` |
| P0.5 假门禁 | ⚠️ **完成但方案被推翻** | `commitment_satisfied` 改为结构判据（原词法匹配实测在中文上恒假，已撤回） |
| P1 动态叙事记忆 | ✅ | `pipeline/memory.py` 接入 `AutoWriter` |
| P2 显式状态转移语义 | ✅ | `validators/transitions.py` + 反例 fixture |
| P3 修复收敛保障 | ✅ | `RepairBudget` 四轴接入 `CriticLoop` |
| P4 校验器自检 | ✅ | 35 个门禁各有 1 正例 + 1 负例，`verify.py` §10 出 precision/recall |
| P5 商业化工程 | 🟡 **3/4 → 3.5/4** | 报告导出 / 证据包 / 备案材料 ✅；**插件契约层已交付，薄客户端本体未做** |
| P6 2.0 互动叙事 | 🟡 部分 | 分支一致性校验 ✅；**存档与 IR 版本绑定未做** |

### 一条必须说清的负面结论

**主题兑现不可机判。** 曾用「承诺措辞 vs 场景文本的词法匹配」当代理，
实测在中文上**恒假**（7 条承诺 7/7 误报，健康分 92 → 21）。
退一步用字符二元组也不行：实测重叠率只有 0.05–0.10，**没有任何阈值**能把
「兑现了」与「没兑现」分开 —— 因为承诺措辞与场景措辞是**两套独立生成的词表**。

L3 真正的可测执行力在 `drift.drift_guard`（测「尾窗还在不在论点上」）。
这条写下来是为了防止有人再把那个假判据装回去。

---

## 4. 第一分钟路径（`实测`）

```
$ keel write "一个外科医生发现自己缝合的每一针都在改写病人的记忆" --out out/_firstrun

real    0m0.504s          ← 离线参考生成器，不需要 API key
已写入 out/_firstrun/：ir.json · novel.md · outline.md · audience.json ·
                      report.html · glance.html · c2pa.json · keel.html
```

产物自包含性（交付主张的核心，逐项验过）：

| 产物 | 体积 | 外链 | `<script>` | 断网可开 |
|---|---|---|---|---|
| `report.html` | 12,804 B | **0** | **0** | ✅ |
| `glance.html` | 4,711 B | **0** | **0** | ✅ |
| `keel.html`（枢纽） | 13,564 B | **0** | 1（内联，无网络请求） | ✅ |

**为什么这件事重要**：报告一旦依赖服务端签名，就变成「信不信我们」。
离线可验 = 谁都能复算，这才是可验证的本义。

---

## 5. 交付物清单

```
keel/                    75 个模块（三层 IR / 校验器 / 渲染器 / 流水线 / 溯源）
tests/                   35 个零依赖自运行测试 + fixtures + 门禁自检样本集
scripts/                 verify.py（自证）· demo.py · pipeline_demo.py
                         check_launch.py · build_zipapp.py · keel.bat · keel.sh
examples/                可运行的示例
keel.pyz                 单文件可执行包
pyproject.toml           打包元数据（packages.find 派生，不手抄）
LICENSE                  MIT
README.md / README_EN.md 中英双版
QUICKSTART.md            5 分钟上手（文中每条命令均实跑过）
CHANGELOG.md             版本记录
ENGINEERING_PLAN.md      工程计划
交付方案_最终.md          本文
+ 6 份设计与调研文档      产品战略 / 第一性原理 / 理论覆盖清单 / 外部调研 ×2 /
                         剧本系统设计评审 / 成为全球顶级项目的路径
```

---

## 6. 还差什么

| # | 缺口 | 级别 | 实测依据 | 补法 |
|---|---|---|---|---|
| **G1** | **P5 薄客户端本体**（VS Code / Cursor / Obsidian） | 🟠 高 | 契约层（`keel/api.py` + `keel api`）已交付并测过 7 条错误路径；**插件本体未做** | 按契约写薄客户端；反漂移测试断言「插件声明的方法 ⊆ 引擎方法」 |
| **G2** | **P6 存档与 IR 版本绑定** | 🟡 中 | `checkpoint.py` 只做检查点，未把存档绑到 IR 版本 | 存档里记 IR 版本指纹，载入时校验 |
| **G3** | **真 LLM 端到端验证** | 🟠 高 | 全部验收都走离线参考生成器 | 需要 API key。**这是唯一能证明生成质量的路** |
| **G4** | `max_rounds` 的质量测量 | 🟡 中 | 只覆盖了**循环机制**（收敛性），**不覆盖生成质量** | 同 G3，需真 LLM |
| **G5** | 打包元数据 `authors` 字段是 `Keel` | 🟢 低 | 作者应是**人**，不是产品名 | 填真实署名 |
| **G6** | 商标检索未做 | 🟠 高（发布前） | §1 只做了软件重名调研 | 正式发布前补 |

### 一个安装上的真实坑（`实测` 踩到）

**Python 3.13 新建的 venv 不含 `setuptools`**，因此 `pip install -e .`
在**离线环境**下会以 `BackendUnavailable: Cannot import 'setuptools.build_meta'` 失败。
两条解法：

```bash
# 有网：让 pip 自己拉构建依赖（默认行为，什么都不用改）
pip install -e .

# 离线：先装 setuptools，再跳过构建隔离
pip install setuptools wheel && pip install -e . --no-build-isolation
```

> 这条写进交付方案，是因为**文档里写错的安装命令比没有文档更贵** ——
> 用户按它做、做不通、就走了。

---

## 7. 验收标准

| 判据 | 阈值 | 当前 |
|---|---|---|
| `scripts/verify.py` | 失败 0 | ✅ **87 · 0** |
| 全部测试文件 | 全绿 | ✅ **35 / 35** |
| 两个端到端 demo | exit 0 | ✅ |
| `check_launch.py` | ALL PASS（无裸 traceback） | ✅ |
| `keel.pyz` | 可独立运行 | ✅ `keel 1.0.0` |
| `report.html` | 0 外链 / 0 脚本 | ✅ |
| 全新目录安装后跑通 | 不依赖当前工作区 | ✅（本次在 `D:\Claude\projects\keel` 重装重跑） |
| 无 API key 端到端 | 必须跑通 | ✅（项目铁律 6） |

**「干净基线全绿」不等于「所有分支都跑过」** —— 所以每条门禁还要求一个反例
fixture，判据是**严重度升级**（变异后严重度 > 基线严重度），不是「变异后触发了」。

---

## 8. 不做什么（四条红线 + 三条防过度工程）

| 不做 | 为什么 |
|---|---|
| **不做网文代笔** | 起点/番茄/晋江一致禁止 AI 直出正文。**同一套 Keel：做网文是违规工具，做剧本是合规工具。** 已实现为 `policy.py` 闸门 |
| **不预测爆款** | LLM 评委与人类判断**反相关**（会把零样本 AI 故事排在《纽约客》之上）。只做**结构性淘汰** |
| **不做反检测** | 技术上徒劳、伦理上站不住、商业上致命。机检红线见 `tests/test_redlines.py` |
| **不自动改结构** | 只自动修**文风**；结构类出提案等人裁决。理由一：保护作者意图。理由二（PLOTTER 实测）：图规划路线在前提忠实度上只有 **40%** |

| 暂不做 | 为什么 |
|---|---|
| Web UI / SaaS | 部署 + 鉴权 + 成本，与当前规模不匹配的大跃迁 |
| 账号 / 计费 | 同上。且定价**不该按生成量**（那等于激励生产更多失败品） |
| 更多校验器 | 35 个门禁已足够。瓶颈是**能不能被看见**，不是检查数量 |

---

## 9. 风险与未闭合项

### 🔴 未闭合：公开仓库的历史泄露（需你决定）

**事实（`实测`）**：旧仓库名 `loom` 的**初始提交 `48cab2af0ccf…`** 里，
误提交了 5 份内部记忆文件（约 185 KB 的项目策略/合规/竞品笔记）。

**已做的处置**：

- 用 `git filter-branch` 重写全部历史，`.workbuddy-ai/**` 从所有分支清除
- 本地对象库 `gc --prune=now`，旧提交**已不可读**
- force-push，远端 4 个提交全部干净

**但仍然没关掉的**：旧提交与其 5 个 blob 在 GitHub 上**仍可经 SHA 取回**
（`git fetch origin <sha>` 成功；API 仍返回 41779 字节）。
**GitHub 只在自家 gc 时丢弃不可达对象，用户无法触发。**

> **「历史已重写」不等于「内容已消失」。** 前者是「没人能顺着分支找到」，
> 后者是「谁都拿不回来」。记录过 SHA 的人仍能原样取回。

**两条出路**：① 删库重建（彻底，但当前 token 无 `delete_repo` scope）；
② 找 GitHub Support 跑一次 gc（保留仓库，耗时不定）。

**缓解事实**：仓库创建于 2026-09-19T09:40Z，**0 fork / 0 star / 0 watcher**，
暴露窗口约 1 小时；且已扫 `sk-` / `gho_` / `ghp_` / `AKIA` / `PRIVATE KEY`，**零命中** ——
**没有凭据泄露**，风险落在「内部笔记被读过」，不是「密钥失窃」。

### 🟠 其他

| 风险 | 说明 |
|---|---|
| 生成质量未经真 LLM 验证 | 全部验收走离线参考生成器。**形态对、内容假**。G3 未做之前，不要对外宣称生成质量 |
| 备案材料涉及法规解释 | 已按「**只做清单不做保证**」实现，并附「以主管部门口径为准」 |
| 名字未经商标检索 | 见 §1 |

---

## 10. 交付之后

1. **补 G1**：按 `keel api` 契约写 VS Code / Cursor / Obsidian 薄客户端
2. **补 G3**：接真 LLM 做端到端验证 —— 这是唯一能证明生成质量的路
3. **关掉 §9 的泄露**：删库重建 或 GitHub Support
4. **发布前**：商标检索 + `authors` 填真实署名

---

## 附录：数字从哪来（可复算）

```bash
cd D:/Claude/projects/keel
.venv/Scripts/python.exe scripts/verify.py                 # 87 · 0
for f in tests/test_*.py; do .venv/Scripts/python.exe "$f"; done   # 34/34
.venv/Scripts/python.exe -c "from keel.validators import registry_stats as s; print(s())"
.venv/Scripts/python.exe -c "from keel import api; print(api.API_VERSION, len(api.METHODS))"
```

**任何一条对不上，这份方案就是错的** —— 请按上面重跑，不要相信本文的数字。
