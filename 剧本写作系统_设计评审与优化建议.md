# 剧本写作系统 1.0 → 2.0：设计评审与优化建议

> 结论先行版。本文结合创作方法论（Aristotle → McKee → Truby → Snyder → Story Grid → Dramatica）、LLM 长文生成的前沿工程共识（Dramatron / Re3 / RecurrentGPT / StoryWriter / DOC / Ex3 / Collective Critics），以及交互叙事的成熟范式（Ashwell 分支拓扑 / Ren'Py / Ink / IBSEN / Story2Game / Generative Agents），对你提出的"1.0 想法→文本、2.0 镶嵌模块做互动小说·漫画·galgame"的思路做一次完整体检。

---

## 0. 结论先行

三句话概括我的判断：

1. **你的 1.0 描述是一个"功能描述"，不是一个"架构"。** "想法输入 → 文本输出"作为产品叙事没问题，但作为技术目标会埋下致命隐患：如果 1.0 的产物是"正文文本"，2.0 一定会推倒重来。

2. **真正的分水岭在于：1.0 的产物是「文本」还是「结构化的故事中间表示」。** 所有能跑通长文本的系统——无一例外——都收敛到同一个模式：**显式中间表示 + 分层规划 + 有界记忆 + 复审循环**。这不是某家的做法，是这个问题的唯一解。

3. **我的核心建议：把 1.0 重新定义为「叙事编译器」（Narrative Compiler）。** 输入是想法，输出不是文本，而是 **Narrative IR（叙事中间表示）**；文本只是 IR 的一个渲染视图。这样 2.0 就不是"镶嵌其他模块"，而是"给同一个 IR 加新的 renderer"——互动小说、galgame、漫画分镜全部免费继承。**2.0 从重写变成扩展，这是整个设计里价值最高的一次重构。**

---

## 1. 你的设计思路体检：哪里对，哪里埋着雷

### 1.1 判断正确的部分

| 你的决策 | 评价 |
|---|---|
| 分阶段（1.0 文本、2.0 互动） | ✅ 正确。1.0 就做多分支会指数爆炸，做不了也做不好 |
| 从"想法输入"起步 | ✅ 正确且高级。比"选个题材"起点高得多，天然贴合"创意辅助"定位 |
| 吸收顶级方法论 + 开源项目 | ✅ 方向对，但执行方式极易走偏（见下方雷 1、雷 3） |
| 2.0 做互动小说 / 漫画 / galgame | ✅ 媒介选择很聪明。这三者的共同点是**文本驱动 + 脚本可被 LLM 直接生成**，适配成本远低于游戏或影视 |

### 1.2 三颗埋着的雷

**雷 1：把方法论当成"提示词模板"**

最常见的失败做法：把 Save the Cat 的 15 个节拍写成一段 prompt，让模型按节拍生成。结果是**形似神不似**——节拍位置对了，但节拍之间没有因果驱动力，读起来像"填空"。

方法论其实有两个身份，缺一不可：

- **生成侧（结构骨架的求解约束）**：不是"顺着写"，而是**倒着设计**。先锁定结局和关键转折（Snyder 的 Final Image、Truby 的 moral argument、McKee 的 controlling idea），再反推节拍。绝大多数结构崩坏，都是因为开写时结局还没定。
- **校验侧（结构体检的判据）**：McKee 的核心断言是"每个场景都是一次价值转折（value charge flips）"；Story Grid 把它量化成"场景必须改变某个价值的状态"；Truby 要求每一步都在推进人物的道德论证。**这些是校验规则，不是写作模板。**

> 一句话：方法论应该是**约束求解器 + 校验器**，不是 prompt 前缀。

**雷 2：把"输出文本"当终点，导致 2.0 无法继承（最致命）**

假设你 1.0 的数据模型是"章节列表 + 正文"。现在要做 galgame，你突然需要：分支点、状态变量、好感度、flag、路线拓扑、立绘资产、场景背景、BGM 触发点。

这些信息在纯文本里全部是**隐含的**，只能让模型重新去"猜"——成本极高、结果不可靠、且无法保证与原文一致。

**正确的顺序是：数据模型必须先于渲染。** 文本是 IR 的一个视图，galgame 脚本是 IR 的另一个视图。这个决定必须在你写第一行代码前做完，否则 2.0 一定重写。

**雷 3：把"吸收 GitHub 顶级项目"当成代码拼装**

这些项目大多解决的是"单篇长文的连贯性"，**不是"可复用的叙事资产"**。它们的价值不在代码，而在**设计模式**：

- Dramatron 的价值：每一步都产出**可读、可编辑的中间产物**，人可以在任何一层介入
- StoryWriter 的价值：**按当前事件选择性压缩历史**，而不是把全部上下文塞进 window
- Re3 的价值：**"写"和"改"是两个独立阶段**，不能混在一起
- Story2Game 的价值：给每个事件标注 **preconditions / effects**，故事就能被编译成可执行逻辑

**抄架构，不抄代码。**

---

## 2. 顶级思考的地图：三个圈子

### 圈子 A — 创作方法论（人类百年积累，注意它们不在同一层）

| 方法论 | 抽象层 | 核心贡献 | 在本系统中的角色 |
|---|---|---|---|
| Aristotle《诗学》 | 原理 | 情节统一性、突转与发现、必然律 | 因果链校验：每个事件是否"必然或可能" |
| Egri《戏剧写作的艺术》 | 内核 | **前提（premise）**、三维角色、冲突升级 | 内核层：把主题写成一句因果断言 |
| Campbell / Vogler | 宏观 | 英雄之旅 12 阶段 | 长篇 / 奇幻的结构模板 |
| Syd Field | 宏观 | 三幕 + 两个情节点 | 最基础的骨架，作为 fallback |
| McKee《故事》 | 场景 | 激励事件、**价值转折**、控制理念 | **场景层校验（最重要的一条）** |
| Truby《故事写作大师班》 | 人物 | 22 步 + 7 个关键结构步骤 + 道德论证 | 人物弧光与主题论证的一致性检查 |
| Snyder《Save the Cat》 | 节拍 | 15 节拍 + 10 种类型分类 | **最适合工程化的骨架**，1.0 首选 |
| Story Grid（Coyne） | 场景 / 序列 | 五个命令、场景与序列的 value shift | 可量化校验：每个单元必须有价值变化 |
| Dramatica | 元结构 | 8 个动态元素、4 条 throughline | 做**结构分析器**，不要做生成器（过于复杂） |
| Harmon Story Circle | 循环 | 8 步循环（单集 / 短片） | 单元剧、短视频、单章结构 |
| 起承转合（Kishōtenketsu） | 四段 | **无冲突的转折结构** | 非西方叙事、日常系、galgame 日常线必备 |
| MICE Quotient（Card） | 判据 | Milieu / Idea / Character / Event 四种"故事问题" | **判断"这个故事什么时候该结束"，防烂尾** |
| Sequence Approach（Gulino） | 中间层 | 8 个序列 | 幕与场景之间的中间粒度 |
| Yorke《Into the Woods》 | 节奏 | 五幕、故事是"变化"的隐喻 | 节奏诊断 |

**关键洞察：这些方法论不是互斥选项，而是不同抽象层的投影。** 系统不该"选一个"，而该建一个多层 schema，让方法论作为**层模板**可插拔——同一套内核，可以套 Save the Cat，也可以套起承转合。

**但要克制：1.0 只做三层（内核 / 节拍 / 场景），其余方法论以"校验器"身份按需启用。** 一次全上，模型会在互相冲突的约束间平均化，结果什么都不像。

### 圈子 B — LLM 长文生成的工程共识

| 项目 | 机制 | 该抄什么 |
|---|---|---|
| **Dramatron**（DeepMind） | 层次化生成，产出 title / characters / plot / location / dialogue 等显式 logs | 每一步产出可读可编辑的中间产物，人在任意层可介入 |
| **Re3**（EMNLP 2022） | Plan → Draft → Rewrite → Edit + 递归重规划 | 生成与修订分离 |
| **RecurrentGPT** | 把长文压缩成语言形式的记忆，循环注入 | 记忆是"生成出来的"，不是"截断出来的" |
| **DOC**（Detailed Outline Control） | 在详细大纲层做控制，而非正文层 | 控制点越靠上游，成本越低、效果越好 |
| **StoryWriter**（THU-KEG, 2025） | Outline Agent → Planning Agent → Writing Agent，Writing Agent 做**动态历史压缩** | 三 Agent 分工 + 按当前事件选择性取用历史 |
| **LongWriter / AgentWrite** | 先规划再分段写，突破单次输出长度 | 分段生成 + 全局计划锚定 |
| **Ex3**（ACL 2024） | Extract → Excelsior → Expand | 从已有文本抽骨架再扩写（做改写/续写很有用） |
| **Collective Critics**（EMNLP 2024） | 多评论家协同批判再修订 | 评审 Agent 群，而非单一"润色" |
| **Awesome-Story-Generation** | 9 大分类全景索引（Plan-and-Write / Multi-Agent / Controllable / Evaluation / Dataset） | 长期跟踪的论文地图 |

**工程共识（全篇最重要的一段）：**

任何能跑通长文的系统，都长这四样东西：

1. **显式中间表示**——绝不允许 idea → text 直连
2. **分层规划**——越往上越抽象，越往上越早锁定
3. **有界记忆**——结构化硬约束 + 检索软记忆 + 滚动摘要，三者分工
4. **复审循环**——生成不等于完成，必须有独立的批判/校验阶段

**把这四样做对，你的 1.0 就已经超过市面上 90% 的"AI 写小说"产品。**

### 圈子 C — 交互叙事（2.0 的真正战场）

**最重要的参考文献：Sam Kabo Ashwell《Standard Patterns in Choice-Based Games》** —— 8 种分支拓扑：Time Cave（时间洞穴）/ Gauntlet（通道）/ Branch and Bottleneck（分支-瓶颈）/ Quest（任务）/ Open Map（开放地图）/ Sorting Hat（分院帽）/ Floating Modules（浮动模块）/ Loop and Grow（循环生长）。

> 核心洞察：**分支拓扑本身是有语义的。选错拓扑比写错台词更致命。** 例如"分院帽"适合阵营选择类 galgame，"分支-瓶颈"适合主线剧情 + 局部分歧，"时间洞穴"只适合短篇（分支数指数爆炸）。

**引擎层选型**（好消息：成本远低于你的预期）：

| 媒介 | 首选 | 理由 |
|---|---|---|
| 互动小说（纯文本分支） | **Ink**（inkle, MIT, 4k★） | **内容与呈现分离**：一份 .ink 同时驱动 3D 游戏和网页。语法极简，LLM 可直接输出 |
| Galgame / 视觉小说 | **Ren'Py**（MIT, 5k★） | 工业标准，内建 Python 可直接调 LLM API，一键出 Windows/macOS/Android/Web |
| 对话密集型游戏 | **Yarn Spinner** | Unity 生态最成熟，社区已有 LLM 自动编写 Yarn 的实践 |
| 纯 HTML 分支故事 | **Twine** | 零门槛，任何浏览器可打开，适合轻量发布 |
| 文学性交互小说 | **Inform 7** | 代码读起来像自然语言，LLM 输出最自然 |
| 商业 galgame | **ChoiceScript** | 路线/好感度/存档机制现成 |

**Agent 层范式**：

- **IBSEN**（ACL 2024）：**导演-演员双轨**。导演管全局情节与约束，演员管角色行为与台词。→ 直接对应 2.0 的"叙事导演 + NPC 智能体"架构
- **StoryVerse**（FDG 2024）：LLM 角色模拟 + 叙事规划协同编辑动态情节
- **Generative Agents**（Stanford）：记忆流 + 反思 + 规划 —— NPC 长期人格一致性的标准答案
- **Story2Game**（2025）：给每个事件标注 **preconditions / effects**，再编译成可执行游戏逻辑。→ **这是 1.0 → 2.0 "编译"思路的最佳实证，也是我最建议你抄的机制**
- **Façade / Versu / Comme il Faut**（Expressive Intelligence Studio）：社交物理引擎，超前的角色自主性设计
- **SOTOPIA / Character-LLM / ChatHaruhi**：角色扮演能力与社交智能评估

**一条容易被忽视的原则**（Janet Murray《Hamlet on the Holodeck》）：**能动性（agency）≠ 选择数量。** 玩家需要的是"可理解、可预期、有意义"的结果，而不是无限生成。这直接否定了"2.0 做 AI 实时生成一切"的路线。

---

## 3. 核心建议：把 1.0 重新定义为「叙事编译器」

> **Idea → Narrative IR → Renderers**

### 3.1 Narrative IR 应该包含什么

分层数据结构，建议 JSON / YAML 持久化 + schema 版本化：

| 层 | 内容 |
|---|---|
| **Premise 层** | logline、主题断言（Egri premise）、控制理念（McKee）、结局、类型、基调、目标媒介与长度 |
| **World 层（Story Bible）** | 实体（角色 / 地点 / 物品 / 组织 / 概念）、实体属性、关系图、世界规则（硬约束） |
| **Character 层** | 欲望 vs 需求（want / need）、缺陷、弧光曲线、初始状态、**状态时间线** |
| **Structure 层** | 幕 / 序列 / 节拍（可插拔模板）、事件节点、**因果边** |
| **Scene 层** | **场景卡** = 场景目标（谁想要什么）+ 冲突（谁/什么阻碍）+ **转折（价值变化）** + 结果（yes / but / no）+ 进出条件 + 涉及实体 + 情绪值 |
| **Ledger 层（台账）** | 伏笔埋设与回收、秘密揭示状态、**知情状态矩阵（谁知道什么）**、时间线 |
| **Provenance 层** | 每段的生成来源、模型、提示词版本、人工编辑痕迹 |

### 3.2 为什么 IR 是整个系统的胜负手

1. **一致性从"事后检测"变成"事前约束"**——写作时只注入本章相关实体与当前状态，而不是把全文塞进 context（这正是 StoryWriter 的"动态历史压缩"）
2. **方法论变成可插拔的结构模板**——同一内核可以套 Save the Cat，也可以套起承转合
3. **2.0 免费获得**——同一个 IR，换个 renderer 就是 galgame 脚本
4. **可评估**——在 IR 层跑结构体检（节拍完整性、伏笔回收率、弧光一致性），不用读全文
5. **可协作**——人类在 IR 层修改，成本是改文本的 1/100
6. **可版本管理**——像 Git 一样 diff 故事，而不是 diff 十万字正文

---

## 4. 分阶段路线图

### 1.0 —— 叙事编译器（文本输出）

**必须交付：**

- [ ] **IR schema v1**（Pydantic / JSON Schema 定义 + 版本号）
- [ ] **三个核心 Agent**：Architect（内核 + 结构）、Scripter（场景卡 → 正文）、Critic（校验）
- [ ] **结构模板插件机制**（先支持 2 个：Save the Cat 15 节拍、三幕 + 起承转合）
- [ ] **一致性引擎 v1**：实体台账 + 状态注入 + 事后 diff 审计
- [ ] **人机协作位点**：内核确认 → 节拍表确认 → 逐章确认（**在 IR 层确认，不是文本层**）
- [ ] **反 slop 模块**：n-gram 重复率、比喻密度、情绪直述、句式单一化检测
- [ ] **"故事体检报告"**：面向用户的可视化交付物（这是核心卖点，不是"一键生成"）

**明确声明 1.0 不做：** 多分支、图像生成、实时对话、多人协作、云端同步。

### 1.5 —— 结构可导出（关键的过渡阶段）

- [ ] IR 导出为 **Ink / Yarn 骨架**（单线 + 预留分支点）
- [ ] 场景卡增加 **preconditions / effects** 字段（Story2Game 做法，直接为 2.0 铺路）
- [ ] 在场景卡上标注"**可分支点**"（即使 1.0 不消费，也要先能标出来）
- [ ] 事件图增加"**状态变量**"概念（好感度、flag 的原型）

### 2.0 —— 多后端渲染

- [ ] **互动小说后端**：Ink（首选）
- [ ] **Galgame 后端**：Ren'Py（路线图 + 好感度/flag 状态机 + 立绘与背景资产清单自动生成）
- [ ] **漫画后端**：分镜 JSON（场景卡 → 镜头表 → 角色一致性 prompt；图像生成走外部模型 + 角色 LoRA / 参考图）
- [ ] **NPC 智能体层**：IBSEN 导演-演员 + Generative Agents 记忆流
- [ ] **分支拓扑模板库**：Ashwell 的 8 种模式，作为可选的"叙事拓扑插件"
- [ ] **可玩性校验**：分支可达性、死路检测、结局覆盖率、状态变量冲突检测

---

## 5. 可执行优化清单

### P0 —— 不做会返工

1. **先写 IR schema，再写 prompt。** schema 是产品，prompt 是耗材。
2. **场景卡必须包含 McKee 的价值转折字段**，并作为硬校验项（场景结束时价值状态必须改变）。
3. **生成顺序改为"倒着设计，正着写"**：先定结局与关键转折，再反推节拍。
4. **记忆分层**：硬约束（结构化 IR 字段，直接注入）/ 软记忆（向量检索，召回相关片段）/ 滚动摘要（近期原文），三者分工明确。
5. **独立的 Critic 阶段，且 Critic 用不同模型或不同系统提示**——避免自我确认偏差（同模型自评倾向于给高分）。
6. **每章写作前注入"本章相关实体 + 当前状态 + 待回收伏笔"**，绝不注入全文。
7. **伏笔台账**：埋设即登记，标注预计回收点，未回收自动报警。

### P1 —— 决定体验上限

8. **结构模板插件化**：一个方法论 = `{ beats[], validators[], prompts[] }` 三件套。
9. **"活大纲"机制**：每完成一个单元，回头修正下一单元的大纲。参考三级大纲实践——**总纲稳定、卷纲每卷重写、章纲每章微调**（基于实际写出的内容，而非最初设想）。
10. **设定审计**：每 N 章做一次全量扫描（时间线、年龄、地理距离、秘密揭示状态）。
11. **评估内建**：内部 rubric（结构完整性 / 一致性 / 文风 / 情感曲线）+ 定期外部对标（EQ-Bench Creative Writing v3、LitBench、LongEval、BooookScore）。
12. **反 slop 检测器做成独立可配置模块**（阈值可调、可开关）。
13. **提示词与模型版本化**——同一部长篇中途换模型会导致风格断裂，必须可追溯、可 A/B。
14. **人机协作点放在 IR 层**，不是文本层。
15. **事件带 preconditions / effects**（2.0 的地基）。
16. **给"选择点"和"状态变量"预留字段**，即使 1.0 不消费。

### P2 —— 差异化竞争点

17. **类型分类器**：Snyder 的 10 种故事类型 / 网文流派 → 自动推荐结构模板。
18. **情感曲线可视化**：逐场景情绪值，直接作为用户的"体检报告"。
19. **多方案生成 + 对比选择**：同一场景生成 3 版让用户选，把选择反馈沉淀为偏好数据。
20. **风格指纹**：从参考作品抽取句长分布、比喻密度、视角习惯，作为渲染层参数。
21. **IR 的 diff 与版本管理**：像 Git 一样管理故事。
22. **"故事医生"模式**：用户贴入自己的大纲/正文，系统输出结构诊断——这是获客利器，也是 IR 能力的最佳展示。

---

## 6. 要避开的坑

| # | 坑 | 为什么致命 |
|---|---|---|
| 1 | idea → 全文直连 | 3 万字以后必然崩坏，且无法局部修复 |
| 2 | 把所有方法论塞进一个超长 prompt | 模型会平均化冲突约束，结果什么都不像 |
| 3 | 把一致性做成事后正则 / 规则 | 必须"状态注入 + 事后审计"双保险 |
| 4 | 1.0 就做多分支 | 分支数指数爆炸，1.0 做不了也做不好 |
| 5 | 2.0 做"AI 实时生成一切" | 玩家要的是有意义的能动性，不是无限生成（Murray） |
| 6 | 把评估交给感觉 | 没有 rubric 就没有迭代方向 |
| 7 | 抄开源项目的代码 | 抄它的**中间表示设计**，代码是次要的 |
| 8 | 单一模型既写又评 | 自我确认偏差，质量天花板很低 |

---

## 7. 三个需要你先决策的问题

这三个问题的答案会显著改变 1.0 的重心，建议在动手前定下来：

1. **媒介定位：1.0 输出"小说/网文"还是"影视剧本"？**
   前者重文风与连载钩子（Ledger 层和章节节奏是核心）；后者重场景、对白与格式（Scene 层和 Dialogue 层是核心）。IR 的侧重点完全不同。

2. **用户角色："AI 写给你看"还是"AI 帮你写"？**
   前者重生成质量与一键体验；后者重 IR 的编辑体验与可控性。**这决定 1.0 是做"生成器"还是"创作工具"。**

3. **IR 是否对用户开放编辑？**
   开放 = 变成创作工具，有数据沉淀和迁移成本（有壁垒）；不开放 = 变成生成器，容易被更强的模型替代。

---

## 附录：参考索引

**创作方法论**
- Aristotle《诗学》/ Lajos Egri《戏剧写作的艺术》/ Robert McKee《故事》
- John Truby《故事写作大师班》/ Blake Snyder《Save the Cat》系列
- Shawn Coyne《Story Grid》/ Melanie Anne Phillips《Dramatica》
- John Yorke《Into the Woods》/ Paul Gulino《Screenwriting: The Sequence Approach》
- Orson Scott Card《Characters & Viewpoint》（MICE Quotient）

**开源项目与论文**
- [google-deepmind/dramatron](https://github.com/google-deepmind/dramatron)
- [THU-KEG/StoryWriter](https://github.com/THU-KEG/StoryWriter)
- [THUDM/LongWriter](https://github.com/THUDM/LongWriter)
- [YILING0013/AI_NovelGenerator](https://github.com/YILING0013/AI_NovelGenerator)
- [yingpengma/Awesome-Story-Generation](https://github.com/yingpengma/Awesome-Story-Generation)
- [Anshler/graphify-novel](https://github.com/Anshler/graphify-novel)
- Story2Game: *Generating (Almost) Everything in an Interactive Fiction Game* (arXiv 2505.03547)
- IBSEN: *Director-Actor Agent Collaboration for Controllable and Interactive Drama Script Generation* (ACL 2024)
- *Collective Critics for Creative Story Generation* (EMNLP 2024)
- *Generating Long-form Story Using Dynamic Hierarchical Outlining with Memory-Enhancement* (NAACL 2025)

**交互叙事**
- [Sam Kabo Ashwell: Standard Patterns in Choice-Based Games](https://www.ifwiki.org/Standard_Patterns_in_Choice-Based_Games)
- [inkle/ink](https://github.com/inkle/ink) · [renpy/renpy](https://github.com/renpy/renpy) · [Yarn Spinner](https://yarnspinner.dev/) · [Twine](https://twinery.org/)
- Janet Murray《Hamlet on the Holodeck》
- *Generative Agents: Interactive Simulacra of Human Behavior* (Stanford, 2023)

**评估基准**
- LitBench (EACL 2026) · EQ-Bench Creative Writing v3 · LongEval · BooookScore · StoryER
